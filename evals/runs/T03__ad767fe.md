# Run: T03 · plugin SHA ad767fe · 2026-07-06

**Fixture:** Next.js + NestJS salon-booking monorepo, dev-pipeline-managed. Loyalty discount can stack with a promo discount under a code path in `pricing/discount.service.ts`. No PR open. Tests green on main.

**User message:** "The loyalty discount is wrong — sometimes it comes out way too high at checkout."

---

## 0. Session start (hooks)

```
[SessionStart hook: hooks/session-start.sh]
(no .claude/pipeline-state.json with active task → no ACTIVE PIPELINE line)
(no .claude/.auto-review-pending → no AUTO-REVIEW PENDING line)
(engineering-craft skill refresh: marker < 24h old → skipped)
(.claude/co-review/ absent → no co-review nudge; Rule 21: opt-in only)
```

Per `CLAUDE.md` line 3: skimmed `CHANGELOG.md` at session start. In force and relevant to this turn: the 2026-07-06 diet entry (routing table + Rules 1–23 one-liners are normative, full bodies in `docs/RULES.md`), the trivial-change bypass conditions, and the 2026-06-07 entry — the Socratic gate now fires for business/behavioural bugs via `fix` Step 1.5, Scope-Lock Mode B.

---

## 1. Classification (Rule 1 — route first)

The message is a reported bug with a wrong *value* symptom. Routing-table check:

- Not "Debug [intermittent issue]" → the "sometimes" is conditional (depends on cart contents), not flaky/racey; there's a concrete wrong-output report. BUG_FIX, not BUG_FIX_COMPLEX.
- **Trivial-change bypass check: FAILS.** The change touches checkout pricing — a **payment surface** — which is explicitly excluded from the bypass ("NO schema/env/auth/payment/URL-topology surface"). No bypass; route.

> **Classified as BUG_FIX → `/dev-pipeline:fix`**

→ Invoking command: `/dev-pipeline:fix`

---

## 2. `/dev-pipeline:fix` — PHASE 11: Fix Cycle

### Step 1: Intake — direct bug report first, then PR review

Case check in the mandated order:

1. **The user's message describes a concrete bug — YES.** "loyalty discount … comes out way too high at checkout" is a direct report. It is the primary issue.
2. PR exists? — `gh pr list` → none open (fixture). Nothing to ADD from review-analyzer; agent not launched.
3. (n/a)

**Issue list:** `[#1] Loyalty discount over-applies at checkout (sometimes far too high).`

**Intake reproduction (read-only — no fix yet, locates the ambiguity axis for triage):**

```
→ Grep "loyalty" apps/api/src/modules/pricing/
→ Read apps/api/src/modules/pricing/discount.service.ts
```

Found in `computeCheckoutTotal()` (sketch):

```ts
// discount.service.ts (current, buggy path)
const promo   = promoCode ? subtotal * promoRate : 0;
const loyalty = tier ? subtotal * loyaltyRate(tier) : 0;   // ← computed on ORIGINAL subtotal
return subtotal - promo - loyalty;                          // ← both subtracted: additive stacking
```

Reproduced with a unit-level probe: subtotal 1000, promo 20%, Gold loyalty 15% → charged **650** (35% off). "Sometimes" = only when a promo code AND a loyalty tier are both present. Both the quote endpoint (`GET /bookings/quote`) and `createBooking` call this same helper.

### Step 1.5: Bug Triage — business vs technical (Rule 23)

Applying the canonical test (`skills/spec-elicitor/SKILL.md` → "When to run me"):

> Is the correct behaviour self-evident, or is it itself the thing in question?

The report names a **wrong number but not the rule** that should produce the right one. Is stacking itself wrong (best-of)? Or is stacking fine but the base is wrong (sequential)? Or is a cap missing? The desired outcome IS the ambiguity — this is a **business/behavioural bug**. This is the SKILL.md's own worked example class ("the loyalty discount applies twice").

→ Invoking Skill: **`dev-pipeline:spec-elicitor` — Mode B (Scope-Lock)**. 2–4 questions, no file, no five-section tracker.

```
🔧 [dev-pipeline] skill: spec-elicitor — Socratic intent elicitation active
   One question per turn → numbered options.
   Mode A (full SPEC): write SPEC.md when all 5 sections are filled · Mode B (Scope-Lock): 2–4 Qs → 🔒 Intent Lock, no file.
```

**Assistant (turn 1):**

> I reproduced it: the total is only wrong when a promo code and a loyalty tier apply to the same checkout — today both discounts are computed on the original subtotal and both subtracted, so they stack additively (20% promo + 15% loyalty = 35% off). Before I fix it, I need one business decision.
>
> **Q1 — When a promo code AND a loyalty discount both apply to one checkout, what should happen?**
>
> 1. Best-of — apply only the larger of the two discounts
> 2. Sequential — apply the promo first, then loyalty on the already-discounted amount
> 3. Additive stacking is intended, but capped at a maximum total discount %
> 4. Other (please describe)

**User:** `1`

**Assistant (turn 2):**

> Got it — best-of: only the larger discount applies, they never combine.
>
> **Q2 — When the promo wins (loyalty discount is NOT applied), what happens to the customer's loyalty points for that booking?**
>
> 1. Points are neither redeemed nor deducted — loyalty simply didn't participate
> 2. Points are still deducted (customer "spent" them by being eligible)
> 3. Customer chooses at checkout which one to use
> 4. Other (please describe)

**User:** `1`

**Assistant (turn 3):**

> Understood — points only move when the loyalty discount actually wins.
>
> **Q3 — Scope of the correction: what about bookings already quoted/held but not yet paid?**
>
> 1. Forward-only — new quotes/checkouts use the corrected rule; anything already priced keeps its price
> 2. Re-price everything unpaid at next touch
> 3. Other (please describe)

**User:** `1`

**Scope-Lock terminates (3 turns ≤ cap of 4). No file written. Intent Lock printed and handed back to the fix flow:**

```
🔒 Intent Lock — Promo and loyalty discounts never combine at checkout: the single larger discount applies.
Decided: Best-of resolution across all discount sources per checkout; loyalty points are redeemed/deducted
         only when the loyalty discount is the one applied; correction is forward-only (already-priced,
         unpaid bookings keep their quoted price).
Rejected: Additive stacking (the current buggy behaviour); sequential compounding (promo→loyalty on the
          discounted base); deducting points when promo wins; re-pricing existing unpaid bookings.
```

This lock will be folded into the fix's commit and PR body (per fix.md Step 1.5).

*Note: no G1–G4 gate fires here — those belong to the `pipeline`/`plan` phase flows. The fix flow's only stop-and-ask is the Scope-Lock above.*

### Step 2: Fix Issues

**Issue #1 (priority: high — customer-facing money bug).**

**1. Analyze.** Root cause: `computeCheckoutTotal()` composes discount sources additively against the original subtotal. Triggered whenever ≥2 discount sources co-apply.

**2. Think aloud.** This issue is about discount *composition*, not the loyalty rate. The fix is a single resolution point — `resolveDiscount(candidates): AppliedDiscount` returning the best-of winner — because per **Rule 11 (fix the class, not the instance)** the class is "any two discount sources co-applying", not "loyalty + promo specifically."

**Rule 11 sweep — what other inputs share this root cause?**

```
→ Grep "applyDiscount\|loyaltyRate\|promoRate" apps/api/src apps/booking/src
```

- `booking.service.ts → createBooking` and `pricing.controller.ts → GET /bookings/quote` — both call the shared helper → covered by fixing the helper. ✅
- `package.service.ts` — package purchases pass only ever ONE discount source today, but call the same helper → covered defensively by the same fix. ✅
- ⚠️ `apps/booking/src/components/checkout-summary.tsx` — the frontend **re-derives** the preview total client-side by summing promo + loyalty (duplicated composition logic). Noted as in-class; will be verified at review (cross-file reviewer) if I underweight it.

**3. Write/update tests FIRST (Rule 19 discipline):**

- **NEW** `discount.service.spec.ts → "applies only the larger discount when promo and loyalty co-apply"` — written first, confirmed **failing** against current code (expects 800, got 650).
- **NEW** `"deducts loyalty points only when loyalty discount wins"` — failing.
- **Existing** spec `"applies promo and loyalty together"` asserted the old additive behaviour. Per **Rule 19** I do NOT silently rewrite it to agree with new code: the old test now fails *meaningfully* — triaged against the 🔒 Intent Lock, the old expectation IS the reported bug. Deleted with an explicit comment: `// Removed 2026-07-06: additive stacking was the defect (Intent Lock: best-of). See fix(pricing) commit body.`
- **NEW invariant test** (Rule 19 pattern 2): `"total discount never exceeds max(single-source discounts)"` — property-style, survives future composition changes.
- **E2E (Rule 6 — payment/multi-step flow, no deferral):** extended `tests/e2e/booking/checkout.spec.ts` with a promo+loyalty scenario asserting the summary total equals the server quote.

**4. Implement the fix (sketch):**

```ts
// discount.service.ts (fixed)
const candidates = collectDiscountCandidates({ promoCode, tier, subtotal }); // [{source:'promo',amount},{source:'loyalty',amount}]
const applied = resolveDiscount(candidates); // best-of: single winner, ties → promo (deterministic)
// loyalty side-effects keyed off the WINNER, not eligibility:
if (applied?.source === 'loyalty') await this.loyalty.redeem(tenantId, customerId, applied);
return { total: subtotal - (applied?.amount ?? 0), applied };
```

Forward-only scope honoured: quote-time snapshot pricing untouched for already-held bookings.

**5. Launch validator agent:**

```
🤖 [dev-pipeline] spawning: validator — lint + tsc + unit + e2e + build (scope: api, booking)
```

→ Agent: validator (input: "run full suite for api + booking after discount composition fix") — output:

```
## Validation Report

| Check | Status | Details |
|-------|--------|---------|
| lint | PASS | 0 errors, 0 warnings |
| tsc | PASS | 0 errors |
| unit-tests | PASS | 214 passed, 0 failed, 0 skipped (4 new) |
| e2e-tests | PASS | 12 passed, 0 failed (1 new: checkout promo+loyalty) |
| build | PASS | api + booking built |

**Overall: PASS**
```

**6. FAIL loop:** not entered (first run clean — the earlier red runs were the deliberate test-first failures).

**Doc guard (pre-push Gate B) satisfied deliberately, not accidentally:** updated `docs/pricing/discount-rules.md` — recorded the best-of rule, the Intent Lock, and the forward-only decision. (Substantive `apps/*/src` change without a tracked `*.md` would be refused at push.)

### Step 3: Re-deliver (commit → review)

Staged only fix-related files (never `git add -A`):

```
apps/api/src/modules/pricing/discount.service.ts
apps/api/src/modules/pricing/discount.service.spec.ts
apps/api/src/modules/booking/booking.service.ts
tests/e2e/booking/checkout.spec.ts
docs/pricing/discount-rules.md
```

→ Delegating to `/commit` (commit-commands:commit):

```
fix(pricing): resolve co-applying discounts best-of instead of additive stacking

Loyalty and promo discounts were both computed on the original subtotal and
both subtracted — any checkout with a promo code AND a loyalty tier was
over-discounted (the "sometimes way too high" report).

🔒 Intent Lock — Promo and loyalty discounts never combine at checkout: the
single larger discount applies.
Decided: best-of across all discount sources; loyalty points move only when
loyalty wins; forward-only (already-priced unpaid bookings keep their price).
Rejected: additive stacking; sequential compounding; deducting points when
promo wins; re-pricing unpaid bookings.

- Root-cause class (Rule 11): ANY two discount sources co-applying — fixed at
  the single resolveDiscount() composition point (covers quote endpoint,
  createBooking, package purchases).
- Tests: new best-of spec (was failing), points-on-winner spec, invariant
  spec (total discount ≤ max single source), E2E promo+loyalty checkout.
  Old additive-stacking spec deleted with rationale (Rule 19 triage: the old
  expectation was the defect).

Co-Authored-By: Claude <noreply@anthropic.com>
```

`[fix/loyalty-promo-stacking a1b2c3d]` (branched off main first — never commit fixes directly on the default branch).

**post-commit hook fires:**

```
──────────────────────────────────────────────
🔍 AUTO-REVIEW DIRECTIVE — commit a1b2c3d…
   Run /dev-pipeline:review before any further work this session.
   Marker: .claude/.auto-review-pending
──────────────────────────────────────────────
```

(The hook also invalidated any stale `.claude/.last-reviewed-sha` and — because the subject matches `^fix(` — appended entry `JNL-a1b2c3d-20260706` to `.learnings/JOURNAL.md` for later `/dev-pipeline:consolidate-lessons`.)

**Rule 2: the directive executes IMMEDIATELY — before responding to the user.** (The Stop hook `stop-review-guard.sh` would deterministically block ending this turn while `.claude/.auto-review-pending` names an unblessed SHA.)

---

## 3. `/dev-pipeline:review` — pre-push self-review (round 1)

**STEP 1 — Capture the diff:** `BASE=merge-base(HEAD, origin/main)` → 5 files, +212/−96. Reviewing `BASE..a1b2c3d`.

**STEP 1.5 — engineering-craft priors (MANDATORY):** skill present at `~/.claude/skills/engineering-craft` (SessionStart refreshed it). Category greps against the diff: concurrency/CAS — no hits; enumeration-safety — no hits; config-drift — no new `process.env.*` added, no env files touched; silent no-op integration — no hits; security-literal removal — no hits. Ran `checklists/pre-merge-self-review.md` mentally — money-math boxes ticked (integer cents in the fixture service; no float drift introduced). No category triggered.

**STEP 1.5b — knowledge-refs sidecar:** wrote `.claude/knowledge-refs-a1b2c3d.json` with `"knowledgeReferences": []` — the signal that STEP 1.5 ran, even with zero triggered categories.

**STEP 1.6 — SDK/API reality-check:** loose trigger fires (diff adds call-like lines) → invoking `/dev-pipeline:verify-sdk-surface`. Its STEP 0 precise filter: every new call resolves to first-party symbols (`resolveDiscount`, `this.loyalty.redeem`) or Prisma methods already probed in `.claude/sdk-probes/prisma.md`; no new third-party surface, no `*.d.ts` edits → **skips cleanly**. Not blocked.

**STEP 2 — parallel reviewers (spawned concurrently, announced first):**

```
🤖 [dev-pipeline] spawning: assumption-checker — diff-vs-docs drift audit + cross-file backward-trace
🤖 [dev-pipeline] spawning: cross-file-reviewer — consumer/producer/lifecycle traces (load cross-file-reasoning skill first)
🤖 [dev-pipeline] spawning: deep-reviewer — correctness, race conditions, edge cases
🤖 [dev-pipeline] spawning: typescript-reviewer — type safety audit (.ts in diff)
🤖 [dev-pipeline] spawning: test-reviewer — test coverage + mock completeness
🤖 [dev-pipeline] spawning: code-review-plugin — 5-agent sweep (installed)
```

(security-reviewer not spawned — diff touches no auth/env/SQL/user-input sink.)

→ **assumption-checker:** PASS — tenant scoping preserved (`tenantId` still threaded through `redeem`); no URL/env/schema drift.
→ **cross-file-reviewer** (loaded `skills/cross-file-reasoning/SKILL.md` + `FAILURE_MODES.md`; ran the seven traces on every changed symbol):

```yaml
cross-file-reasoning:
  scope: [discount.service.ts, booking.service.ts, checkout-summary.tsx(NOT in diff), discount.service.spec.ts]
  symbols-traced:
    - name: resolveDiscount
      type: exported-function
      trace: producer(discount.service.ts) → consumers(booking.service.ts, pricing.controller.ts, package.service.ts) → all server-side ✓
      verdict: PASS
    - name: checkout total (derived value)
      type: cross-app derived value
      trace: server(resolveDiscount → best-of) vs client(apps/booking/src/components/checkout-summary.tsx:41
             sums promoDiscount + loyaltyDiscount locally) → DIVERGENT derivation
      verdict: BLOCK — client preview still shows the stacked (wrong) total; user sees preview ≠ charge
    - name: LoyaltyService.redeem (call moved behind winner check)
      type: interface-consumer
      trace: specs instantiating BookingService provide redeem mock ✓ (Trace 5 mock-completeness)
      verdict: PASS
  failure-mode-matches: []
  verdict: BLOCK
```

→ **deep-reviewer:** P3 — tie-break (promo wins on equal amounts) deserves a code comment referencing the Intent Lock. P3 — `applied` nullability could use an explicit empty-candidates unit case.
→ **typescript-reviewer:** PASS.
→ **test-reviewer:** PASS — flagged approvingly that the old additive spec was deleted-with-rationale rather than rewritten (Rule 19).
→ **/code-review plugin:** score 88/100, no blocking findings; echoed the client-side duplication.

**STEP 3 — aggregate** → saved `.claude/review-findings-a1b2c3d.md`:

| # | Severity | File:Line | Reviewer | Issue | Proposed Fix |
|---|----------|-----------|----------|-------|--------------|
| 1 | P1 | apps/booking/src/components/checkout-summary.tsx:41 | cross-file | Client re-derives total by summing promo+loyalty — preview still stacks; diverges from server charge | Consume the server `/bookings/quote` response (`total`, `applied`) instead of local composition |
| 2 | P3 | discount.service.ts:58 | deep | Tie-break undocumented | Comment citing Intent Lock |
| 3 | P3 | discount.service.spec.ts | deep | No empty-candidates case | Add unit case |

**STEP 4 — gate decision:** 1 × P1 → **BLOCK.**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🛑 REVIEW BLOCKED — HEAD NOT blessed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
P1 findings: 1  (all must be fixed)
P2 findings: 0
Findings file: .claude/review-findings-a1b2c3d.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Running /dev-pipeline:fix to address P1 findings…
```

**STEP 6 — audit trail:** appended to `.claude/agent-events.jsonl`:
`{"event":"review.complete","sha":"a1b2c3d…","p1":1,"p2":0,"p3":2,"blessed":false,"ts":"2026-07-06T09:41:12Z"}`

---

## 4. `/dev-pipeline:fix` (auto-invoked with findings file) — round 2

Step 1: intake = findings file (technical findings from review — Step 1.5 triage: correct behaviour self-evident, the Intent Lock already decided it → **no second Scope-Lock**; straight to Step 2).

- **#1 (P1):** `checkout-summary.tsx` — removed local `promoDiscount + loyaltyDiscount` math; the summary now renders `quote.total` and a single `quote.applied` discount line from the server quote response. This is the same Rule 11 class closed at its last consumer. No new user-facing strings (renders existing labels) → no i18n keys needed.
- **#2, #3 (P3s):** tie-break comment added; empty-candidates unit case added.
- Test-first: updated `checkout-summary.test.tsx` asserts the rendered total equals the mocked quote payload (not locally derived), + the new E2E from round 1 already covers the seam in a real browser.
- → validator agent: **Overall: PASS** (lint/tsc/unit 217/e2e 12/build all green).

Staged; → `/commit`:

```
fix(booking): checkout summary consumes server quote instead of re-deriving discounts

Review finding P1 (review-findings-a1b2c3d): the client preview summed promo +
loyalty locally — the exact composition bug fixed server-side in a1b2c3d.
Closes the last consumer of the "co-applying discounts composed additively"
class (Rule 11). P3s: tie-break comment (Intent Lock), empty-candidates case.

Co-Authored-By: Claude <noreply@anthropic.com>
```

`[fix/loyalty-promo-stacking e4f5a6b]` → post-commit hook: **AUTO-REVIEW DIRECTIVE — commit e4f5a6b** (blessing for a1b2c3d never existed; marker now points at e4f5a6b). Rule 2 → re-run review now.

---

## 5. `/dev-pipeline:review` — round 2

- STEP 1: diff `BASE..e4f5a6b` (7 files).
- STEP 1.5: same category greps — none fire; sidecar `.claude/knowledge-refs-e4f5a6b.json` (empty refs).
- STEP 1.6: verify-sdk-surface STEP 0 → skips (no new third-party surface).
- STEP 2: same reviewer set re-spawned. cross-file-reviewer re-ran Trace set on the seam:
  `checkout total: server(resolveDiscount) → client(renders quote.total) → single derivation point → PASS`. All reviewers PASS; /code-review score 95.
- STEP 3: findings — 0 P1, 0 P2, 1 P3 (nit: test fixture naming). Saved `.claude/review-findings-e4f5a6b.md`.
- STEP 4: 0 P1, 0 P2 → **bless immediately** (no user ACK needed at this count).
- STEP 5: wrote `e4f5a6b` → `.claude/.last-reviewed-sha`; cleared `.claude/.auto-review-pending`, `.claude/.review-in-flight`.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ REVIEW PASSED — HEAD blessed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SHA:      e4f5a6b
Files:    7
Findings: 0 P1, 0 P2, 1 P3
Blessed:  .claude/.last-reviewed-sha
Next:     git push   OR   /dev-pipeline:deliver
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

- STEP 6: `{"event":"review.complete","sha":"e4f5a6b…","p1":0,"p2":0,"p3":1,"blessed":true,"ts":"2026-07-06T10:02:44Z"}`

Fix summary presented (fix.md OUTPUT): 1 reported bug + 1 review P1 + 2 P3s fixed; 7 files; review clean and blessed. No `task_plan.md` in this session → nothing to update there.

---

## 6. Auto phase transition (Rule 3) → `/dev-pipeline:deliver`

Work for the reported bug is complete, review is clean, no PR exists → per Rule 3 / the Delivery Auto-Trigger conditions, deliver is invoked without waiting to be asked. **Rule 4:** no improvised `git push`/`gh pr merge` outside this flow.

> All fixes complete and blessed. Proceeding to delivery gate → `/dev-pipeline:deliver`.

**PHASE 9 — Commit & PR.**
→ Agent: doc-writer — confirms the tracked doc record covers the shipped change (`docs/pricing/discount-rules.md` updated in commit 1 with the Intent Lock + best-of rule; both commit bodies carry rationale); refreshes the thin pointer `.claude/pipeline-state.json → {task:"loyalty-discount-stacking-fix", branch:"fix/loyalty-promo-stacking", phase:"deliver", pr:pending}`.
Everything already committed in Steps above; → delegating to `/commit-push-pr` for push + PR.
**pre-push hook:** Gate A — HEAD `e4f5a6b` == blessed SHA ✅ · Gate B — push range contains `docs/pricing/discount-rules.md` ✅ → push allowed.
PR **#104** created: *"fix(pricing): best-of discount resolution at checkout (loyalty × promo stacking)"* — body includes the 🔒 Intent Lock verbatim, the Rule 11 class statement, test evidence, and the two review-findings files' summaries.

**PHASE 9.5 — Conflict gate (automatic, no override).** `gh pr view 104 --json mergeable,mergeStateStatus` → `MERGEABLE|CLEAN` on first poll.
`✅ PR 104 mergeable (state: MERGEABLE|CLEAN) — proceeding to review.`

**PHASE 10 — Code review.** → Delegating to `/code-review` on PR #104 → clean (no new findings beyond the already-fixed set). Proceed; do NOT auto-merge yet.

**PHASE 10.5 — Browser E2E gate.** Surface detection: diff touches `apps/booking/src/` → `FRONTEND_TOUCHED=1`, `AFFECTED_APPS=(booking)`. Parsed vercel[bot] comment → `BOOKING_PREVIEW=https://booking-git-fix-loyalty-….vercel.app` (HTTP 200, not auth-protected).
`PLAYWRIGHT_BOOKING_URL=$BOOKING_PREVIEW npx playwright test --project=booking-chromium tests/e2e/booking/` → **12 passed** (incl. the new promo+loyalty checkout spec asserting the rendered total equals the server quote — role/text locators, real clicks).
`✅ E2E gate cleared for booking — proceeding to merge.` → `gh pr merge 104 --squash --auto`.

**PHASE 11 — Monitor merge & CI.** Merged; `gh run watch <run> --exit-status` → CI green. Pointer rotated: `.claude/pipeline-state.json → .claude/archive/pipeline-state-104.json` ("Pointer rotated (PR #104 merged). Durable record is in docs/pricing/.").

**PHASE 12 — Smart deploy.** → `/dev-pipeline:deploy` — project-context: WEB/Vercel adapters (booking, api via its pipeline) → deploys triggered by the merge; adapter confirms deployment READY; per-deploy smoke ping OK.

**PHASE 12.5 — Production smoke.** Waited the 5-minute stabilisation window; resolved prod URLs from `.claude/docs/URL_TOPOLOGY.md`; ran the narrow booking smoke subset (`home.spec.ts`, `tenant-home.spec.ts`) against production → **green**. (Rules 17/20: preview green ≠ production green; verified against PRODUCTION URLs before declaring success.)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ SHIPPED: best-of discount resolution at checkout
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PR:               https://github.com/<org>/<repo>/pull/104
Preview E2E:      ✅ 12/12 passed (booking-chromium)
CI:               ✅ passed
Deploy:           ✅ Vercel (booking) · API pipeline green
Production smoke: ✅ 2/2 passed (after 5min stabilisation)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Closing report to user:** root cause (additive composition of co-applying discount sources), the locked rule (best-of; points move only when loyalty wins; forward-only), the class-level closure incl. the client preview seam, and where the record lives (`docs/pricing/discount-rules.md`, PR #104, `.learnings/JOURNAL.md` entries pending the next `/dev-pipeline:consolidate-lessons` sweep). Per Rule 9 triggers, a post-delivery one-shot `/dev-pipeline:refactor` on the pricing module was OFFERED, not run.

---

## Gate/rule ledger for this run

| Mechanism | Fired? | Where |
|---|---|---|
| Rule 1 routing + announcement | ✅ | §1 (BUG_FIX → fix; trivial bypass correctly refused — payment surface) |
| Rule 23 Socratic gate / spec-elicitor Mode B | ✅ | §2 Step 1.5 (3 Qs, numbered options, 🔒 Intent Lock, no file) |
| Rule 11 class-level fix test | ✅ | §2 Step 2 + closed fully by review P1 |
| Rule 19 test triage (no tautology rewrite) | ✅ | §2 Step 2.3 |
| Rule 6 E2E for payment flow | ✅ | validator e2e + PHASE 10.5 + prod smoke |
| Rule 2/8 AUTO-REVIEW DIRECTIVE → review → blessing | ✅ ×2 | §3–§5 (round 1 BLOCKed on P1; round 2 blessed) |
| STEP 1.5 priors + 1.5b sidecar + 1.6 SDK check | ✅ | §3, §5 (sidecars emitted; verify-sdk-surface skipped cleanly) |
| Pre-push Gate A (blessing) + Gate B (doc guard) | ✅ | §6 PHASE 9 |
| Rule 3/4 auto-deliver, no improvised merge | ✅ | §6 |
| Rule 15 conflict gate · Rule 16 preview E2E · Rules 17/20 prod smoke | ✅ | §6 PHASES 9.5 / 10.5 / 12.5 |
| G1–G4 | n/a | pipeline/plan-flow gates; fix flow uses Scope-Lock instead |
| Rule 21 co-review | not invoked | opt-in; channel not enabled |
