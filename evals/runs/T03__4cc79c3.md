# Run: T03 · plugin SHA 4cc79c3 · 2026-07-06

**Task (user message):** "The loyalty discount is wrong — sometimes it comes out way too high at checkout."
**Fixture:** Next.js + NestJS salon-booking monorepo (dev-pipeline-managed). Bug lives in `apps/api/src/modules/pricing/discount.service.ts` (loyalty discount can stack with promo discount on one code path). No PR open. Tests green on main.
**Instruction source:** `/tmp/dp-baseline/` plugin checkout only (`CLAUDE.md` + `commands/*` + `skills/*` + `agents/*` + `hooks/*`).

---

## SESSION TRANSCRIPT

### [Session start — hooks]

```
[session-start.sh] engineering-craft skill present (~/.claude/skills/engineering-craft, refreshed <24h)
[session-start.sh] pipeline state: .claude/pipeline-state.json → no active task, no in-progress MIUs, no PR
[session-start.sh] no .claude/.auto-review-pending marker · no DEEP_REVIEW_REQUIRED marker
[session-start.sh] co-review: .claude/co-review/enabled NOT present → no nudge (Rule 21: opt-in only)
[session-start.sh] living docs found: .claude/docs/PROJECT_STATUS.md, ARCHITECTURE.md, RECENT_CHANGES.md
```

Per plugin `CLAUDE.md` header ("What's new"), skimmed `CHANGELOG.md`: Rule 22 SDK reality-check (2026-06-30, hardened 07-01), co-review relay (opt-in), Socratic gate widened across fix/update (2026-06-07). All in force.

---

### Turn 1 — user

> The loyalty discount is wrong — sometimes it comes out way too high at checkout.

**Auto-Invocation Decision Tree (checked at start of turn):**
- AUTO-REVIEW DIRECTIVE in hook output? No.
- DEEP_REVIEW_REQUIRED marker? No.
- Active pipeline with in-progress MIUs? No.
- User requesting a code change? **Yes → classify.**

**Classification.** Routing table: "Fix [bug]" → BUG_FIX_SIMPLE. Considered BUG_FIX_COMPLEX ("Debug [intermittent issue]") because of "sometimes" — rejected: the report is not an intermittent technical fault, it's a deterministic wrong *result* on a condition ("sometimes" = whichever inputs hit the stacking path). This is the canonical business/behavioural bug — `CLAUDE.md` Rule 23 and `fix.md` Step 1.5 both literally use "the loyalty discount applies twice" as the worked example.

**Announced:** *"Classified as BUG_FIX_SIMPLE (business/behavioural) → invoking `/dev-pipeline:fix`."*

→ Reading Living Documents before any code work (plugin CLAUDE.md → "Living Documents", rules 1–2):
- `Read .claude/docs/PROJECT_STATUS.md` → branch main, no active task, timestamp matches HEAD (fresh — no `/dev-pipeline:sync` needed)
- `Read .claude/docs/RECENT_CHANGES.md` → last change touched booking confirmation flow; nothing recent in `pricing/`

→ Invoking command: **`/dev-pipeline:fix`**

**Step 1 — Intake (direct report wins).**
Case 1 applies: the user's message describes a concrete bug → the report itself is the primary issue.
```
$ gh pr list --state open
(no open pull requests)
```
No PR → nothing to append from review bots. Issue list = [#1 "loyalty discount sometimes far too high at checkout"].

**Step 1.5 — Bug triage: business vs technical (Rule 23).**
The canonical test (`skills/spec-elicitor/SKILL.md → "When to run me"`): *is the correct behaviour self-evident, or is it itself the thing in question?*
The report names a wrong **result** ("way too high") but not the **rule** that yields the right one — how loyalty should interact with other discounts has never been decided in any doc I can find (`grep -rn "stack\|best.of\|composition" docs/ → no policy hit`). That is a **business bug**. Fixing it without locking intent would ship my own guess (Rule 23's exact failure mode).

→ Invoking Skill: **`dev-pipeline:spec-elicitor`** (Mode B — Scope-Lock: 2–4 questions, no file)

```
🔧 [dev-pipeline] skill: spec-elicitor — Socratic intent elicitation active
   One question per turn → numbered options.
   Mode A (full SPEC): write SPEC.md when all 5 sections are filled · Mode B (Scope-Lock): 2–4 Qs → 🔒 Intent Lock, no file.
```

I've confirmed the symptom in code first (so the questions are concrete, not abstract):
→ `Read apps/api/src/modules/pricing/discount.service.ts`

```ts
// discount.service.ts — applyDiscounts() (sketch of the buggy path)
const promoOff   = promo ? this.computePromo(subtotal, promo) : 0;
const loyaltyOff = customer.loyaltyTier ? subtotal * LOYALTY_PCT[customer.loyaltyTier] : 0;
return { total: subtotal - promoOff - loyaltyOff, ... };   // ← when BOTH are truthy, they stack
```

"Sometimes" decoded: only customers who have a loyalty tier AND enter a promo code hit this branch — both discounts are computed against the full subtotal and summed. One question at a time (elicitor Rule 1; no five-section tracker — Mode B):

**Q1 — When a customer has BOTH a loyalty reward and a promo code at checkout, what should the total reflect?**

1. Stack — both discounts apply in full (this is what the code does today)
2. Best-of — only the single larger discount applies
3. Sequential — promo first, then loyalty % on the already-discounted subtotal
4. Other (please describe)

*(turn ends — one question per turn)*

---

### Turn 2 — user

> 2 — they should never stack. That's why the totals blow up.

Got it — the intended rule is **best-of**: exactly one discount applies, and stacking is the bug, not a feature. (No progress tracker printed — Mode B.)

**Q2 — Promos can be percentage OR fixed-amount vouchers; loyalty is a tier percentage. How do we decide which is "larger"?**

1. By computed saving in dollars on the eligible subtotal (compare actual amounts)
2. Percentage always beats fixed-amount
3. The promo always wins when both are present
4. Other (please describe)

---

### Turn 3 — user

> 1. And if it's an exact tie, prefer the promo — marketing paid for those.

Understood — compare by **computed amount**, tie-break to the **promo**.

**Q3 — Scope of this rule: is best-of a platform rule, or does each salon (tenant) get to choose?**

1. Platform-wide rule now; tenant-level configurability only if a tenant asks (out of scope this round)
2. Add a per-tenant toggle now
3. Other (please describe)

---

### Turn 4 — user

> 1 — platform-wide. Don't build config for this.

Acknowledged. That resolves the single ambiguous axis in ≤4 turns — terminating Scope-Lock (no file written, per Mode B rules):

```
🔒 Intent Lock — At checkout exactly ONE discount applies: whichever of {loyalty reward, promo code}
   yields the larger computed saving on the eligible subtotal; exact ties go to the promo.
   Discounts never stack and never compound.
Decided: best-of by computed amount · tie → promo · platform-wide rule (no tenant toggle this round)
Rejected: stacking (sum of both — the current bug) · sequential/compounding application ·
          per-tenant configurability (deferred; noted in docs as a non-goal)
```

Handing back to `/dev-pipeline:fix`. The Intent Lock will be folded into the commit and PR body (Rule 23 / elicitor Mode B contract).

---

**Step 2 — Fix Issues** (issue #1, priority: high)

**2.1 Analyze — root cause + Rule 11 (fix the class, not the enumeration).**
General class: *multiple discount sources composed with no composition policy* — each source computes independently against the same base and the results are summed. Enumerating other inputs that hit the same class:
```
$ grep -rn "applyDiscounts\|promoOff\|loyaltyOff" apps/api/src/modules --include=*.ts
apps/api/src/modules/pricing/discount.service.ts      (declaration)
apps/api/src/modules/pricing/checkout.service.ts:88   (checkout quote)
apps/api/src/modules/booking/booking-confirm.service.ts:141  (final charge at confirm)
```
Two consumers, one choke point. The class-level fix is a single composition policy inside `DiscountService.applyDiscounts()` — both call sites are covered without per-caller patches. Rule 11 test ("what other inputs would have triggered the same root cause?"): any future discount source (e.g. gift promo campaigns) added as a candidate flows through the same `pickBestDiscount`, so the fix covers the class, not the instance.

**2.2 Think aloud.** This issue is about a missing composition rule. The fix is a `candidates[] → pickBestDiscount()` selection (max by computed cents, tie → promo) at the single choke point, because both the quote path and the confirm path must agree, and a policy function is testable in isolation.

**Plan the technical unit** — the fix flow needs to plan technical work, which triggers the decomposition skill:

→ Invoking Skill: **`dev-pipeline:miu-methodology`**

```
🔧 [dev-pipeline] skill: miu-methodology — MIU decomposition engine active (Phase 3-4)
   Two-level decomposition: Product Tasks → Technical MIUs
```

Product Task (Level 1): "Checkout applies exactly one discount — the larger of loyalty/promo."

```
MIU 1: Best-of discount composition in DiscountService
  Block:        BACKEND
  Files:        apps/api/src/modules/pricing/discount.service.ts,
                apps/api/src/modules/pricing/discount.service.spec.ts
  Type:         modify-existing
  Depends on:   none
  What it does:
    - Replace summed promoOff/loyaltyOff with candidates[] of {source, cents};
      pickBestDiscount() returns max by cents, tie-break source === 'promo'
    - Return shape gains appliedDiscount: {source, cents} | null (additive; both
      consumers read `total`, verified above — no DTO change)
  Build/Deploy/Runtime impact: none — pure logic change inside the API service; no new
    deps, no Dockerfile/vercel.json/CI/workspace-package changes; API image build unaffected.
  Test plan (TDD — write FIRST):
    - "applies only the larger discount when both loyalty and promo are present"
      (loyalty 10% vs promo $5 on $200 → total $180, appliedDiscount.source === 'loyalty')
    - "exact tie prefers the promo" ($20 promo vs $20 loyalty → appliedDiscount.source === 'promo')
    - "loyalty-only and promo-only paths unchanged" (regression)
    - invariant: "total discount never exceeds max(loyalty, promo)" (property-style, Rule 19 pattern 2)
    - e2e: booking checkout with tiered customer + promo code shows best-of total (Rule 6)
  Done when: new specs pass; full pricing suite passes; project compiles; e2e checkout spec green.
```

**Branch first (work must not land directly on main):**
```
$ git checkout -b fix/loyalty-promo-discount-stacking
```

**2.3 Write/update tests FIRST.**
→ `Edit apps/api/src/modules/pricing/discount.service.spec.ts` — added the four unit specs above.
```
$ pnpm --filter api test -- discount.service
✗ applies only the larger discount when both loyalty and promo are present
  (got 155.00, expected 180.00 — stacking reproduced)
✗ exact tie prefers the promo
✓ loyalty-only path unchanged
✓ promo-only path unchanged
```
Failing for the right reason — bug reproduced.

**Rule 19 triage (existing test that asserted the OLD behaviour).** The suite contains `"applies loyalty and promo cumulatively"` — it now fails, and that failure is *meaningful*: the behaviour change is user-visible and intended. Per Rule 19 the triage is explicit: the old behaviour is the bug per the 🔒 Intent Lock, so the old test is deleted **with a comment** (not silently rewritten to agree with the new code):
```ts
// Removed 2026-07-06: asserted loyalty+promo stacking. Stacking was the defect —
// see Intent Lock in fix(api) commit / docs/discount-composition/. Replacement
// coverage: best-of specs + "total discount never exceeds max(...)" invariant below.
```

**Rule 6 (E2E mandatory — this touches the amount charged, i.e. payment):**
→ `Edit tests/e2e/booking/checkout.spec.ts` — added spec: sign in as fixture loyalty-tier customer, apply promo `SUMMER10`, assert rendered total equals best-of amount (uses `page.getByRole`/`getByText`, no testids, no `networkidle` — per Rule 16's "like a user" standard).

**2.4 Implement the fix.**
→ `Edit apps/api/src/modules/pricing/discount.service.ts`
```ts
const candidates = [
  promo                ? { source: 'promo'   as const, amount: this.computePromo(subtotal, promo) }         : null,
  customer.loyaltyTier ? { source: 'loyalty' as const, amount: subtotal * LOYALTY_PCT[customer.loyaltyTier] } : null,
].filter(isDefined);
const applied = pickBestDiscount(candidates);   // max by amount; tie → 'promo'
return { total: subtotal - (applied?.amount ?? 0), appliedDiscount: applied ?? null, ... };
```

**Docs (kept in sync in the same diff; also feeds pre-push Gate B + deliver's doc-writer):**
→ `Write docs/discount-composition/discount-composition-execution.md` — What / Why / Intent Lock verbatim / Tests written / Engineering rationale (why best-of at the service choke point vs per-caller guards; what failed first: the reproduced stacking spec; deferred: tenant-level policy toggle, recorded as follow-up).

**2.5 Launch validator agent.**

→ Spawning Agent: **`validator`** — "Run full validation for the api app + e2e"

```
## Validation Report
| Check      | Status | Details                                              |
|------------|--------|------------------------------------------------------|
| lint       | PASS   | 0 errors, 0 warnings                                 |
| tsc        | PASS   | 0 errors                                             |
| unit-tests | PASS   | 214 passed, 0 failed, 0 skipped                      |
| e2e-tests  | PASS   | 31 passed, 0 failed (incl. new checkout best-of spec)|
| build      | PASS   |                                                      |
**Overall: PASS**
```

**MIU boundary cross-check (Rule 14 — automatic, not opt-in):**

→ Spawning Agent: **`assumption-checker`** — diff-vs-docs drift audit + cross-file backward traces (steps 8–14)

```
ASSUMPTION-CHECK: PASS
- No hardcoded URL/topology/env-var assumptions introduced (no process.env.* in diff).
- Tenant invariant: pricing runs inside tenant-scoped checkout/booking contexts; no new
  queries added — invariant untouched.
- Trace #14 (mock completeness): checkout.service.spec.ts + booking-confirm.service.spec.ts
  mock DiscountService.applyDiscounts — both mocks updated for the additive appliedDiscount
  field, cast as the full interface (verified).
- Doc freshness: behaviour change ships WITH docs/discount-composition/ in the same diff. ✓
```

MIU 1 marked complete in `.claude/miu-progress.json`.

---

**Step 3 — Re-deliver (commit + review loop)**

Stage only fix-related files (never `git add -A`):
```
$ git add apps/api/src/modules/pricing/discount.service.ts \
          apps/api/src/modules/pricing/discount.service.spec.ts \
          tests/e2e/booking/checkout.spec.ts \
          docs/discount-composition/discount-composition-execution.md
```

→ Invoking Skill: **`commit-commands:commit`** (fix flow Step 3.2 — delegate to `/commit`)

```
fix(api): checkout applies best-of discount — loyalty and promo no longer stack

- MIU 1: best-of composition in DiscountService (max by computed amount, tie → promo)
  — customers with a loyalty tier + promo code were getting BOTH discounts summed
- Tests: 4 new unit specs (best-of, tie-break, single-source regressions, never-exceeds-max
  invariant) + e2e checkout best-of spec; removed the stacking spec with triage comment (Rule 19)

🔒 Intent Lock — exactly ONE discount applies at checkout: the larger of {loyalty, promo}
   by computed saving; ties go to the promo. Discounts never stack.
Decided: best-of by amount · tie → promo · platform-wide rule
Rejected: stacking (the bug) · sequential application · per-tenant toggle (deferred)

[fix/loyalty-promo-discount-stacking 3f7a2c1]
```

**post-commit hook fires:**
```
──────────────────────────────────────────────
🔍 AUTO-REVIEW DIRECTIVE — commit 3f7a2c1…
   Run /dev-pipeline:review before any further work this session.
   Marker: .claude/.auto-review-pending
──────────────────────────────────────────────
[journal] fix( prefix matched → entry JNL-3f7a2c1-20260706 appended to .learnings/JOURNAL.md
```

**Rule 2: executing the directive IMMEDIATELY** (fix Step 3.3's `/code-review` re-review is satisfied inside this command — review STEP 2E delegates to the code-review plugin, avoiding a duplicate sweep).

→ Invoking command: **`/dev-pipeline:review`**

**STEP 1 — Capture diff.** `BASE=$(git merge-base HEAD origin/main)` → reviewing `a91c2e4..3f7a2c1` — 4 files, +168/−54.

**STEP 1.5 — engineering-craft priors.** Skill present (SessionStart bootstrapped). Category trigger greps against the diff: concurrency/CAS — no hit; enumeration safety — no hit; config drift (filenames + new env consumers) — no hit; silent no-op integration — no hit; security-literal removal — no hit. No category fires → ran `checklists/pre-merge-self-review.md` mentally (money-math and behaviour-change boxes ticked; no triggered-category boxes open).
**STEP 1.5b** — emitted `.claude/knowledge-refs-3f7a2c1.json` with `"knowledgeReferences": []` (the empty array is the signal STEP 1.5 ran).

**STEP 1.6 — SDK/API reality-check trigger.** Loose trigger fires (`NEED_SDK=1` — diff adds call-like lines).
→ Invoking command: **`/dev-pipeline:verify-sdk-surface`** → STEP 0 precise filter: all imports in the diff are workspace-internal (`pricing/*`, `@luxebook/utils`); no third-party package surface called or stubbed, no `*.d.ts` edited → **skips cleanly**. No probe required.

**STEP 2 — Parallel reviewers (spawned concurrently, announced before each):**
```
🤖 [dev-pipeline] spawning: assumption-checker — diff-vs-docs drift audit + cross-file backward-trace
🤖 [dev-pipeline] spawning: cross-file-reviewer — consumer/producer/lifecycle traces (load cross-file-reasoning skill first)
🤖 [dev-pipeline] spawning: deep-reviewer — correctness, race conditions, edge cases
🤖 [dev-pipeline] spawning: typescript-reviewer — type safety audit (if .ts/.tsx in diff)
🤖 [dev-pipeline] spawning: security-reviewer — auth, env vars, SQL, user input (if applicable)
🤖 [dev-pipeline] spawning: test-reviewer — test coverage + mock completeness
🤖 [dev-pipeline] spawning: code-review-plugin — 5-agent sweep (if installed)
```
- **assumption-checker (A0):** PASS (re-audit of the committed diff; same result as MIU boundary).
- **cross-file-reviewer (A0.5):** PASS — env-var/route/SDK/event/wrapper traces n/a; conditional-coupling trace on `candidates[]` gating clean; mock-completeness verified at both consumer specs. No FAILURE_MODES.md match.
- **deep-reviewer (A):** 2 findings —
  **P2** `discount.service.ts:41` — money computed in floating-point dollars (`subtotal * LOYALTY_PCT[tier]`) *before* the best-of comparison. Codebase precedent is integer-cents math (`packages/utils/src/money.ts → toCents/percentOfCents`). FP error can mis-rank a near-tie and silently violate the locked "tie → promo" rule. Proposed fix: compute candidate amounts in cents, compare integers.
  **P3** `discount.service.ts:12` — JSDoc still says "discounts are cumulative".
- **typescript-reviewer (B):** PASS — no `as any`, discriminated union on `source` sound.
- **security-reviewer (C):** not applicable — diff touches no auth/crypto/env/SQL/user-input sink (promo code validation upstream, unchanged).
- **test-reviewer (D):** PASS — new tests assert the behaviour change; old stacking spec removed with an explanatory triage comment, replaced by an invariant test (no FAILURE_MODES.md #10 tautology); no `.only`/`.skip`; mocks complete.
- **code-review plugin (E):** installed → 5-agent sweep on the diff: score 91/100, no blocking findings (echoes the float-money P2 — deduped).

**STEP 3 — Aggregate findings** → saved `.claude/review-findings-3f7a2c1.md`:

| # | Severity | File:Line | Reviewer | Issue | Proposed Fix |
|---|----------|-----------|----------|-------|--------------|
| 1 | P2 | pricing/discount.service.ts:41 | deep (+plugin, deduped) | Float dollar math before best-of comparison; near-tie mis-ranking can violate "tie → promo" | Compute candidates in integer cents via `@luxebook/utils` money helpers |
| 2 | P3 | pricing/discount.service.ts:12 | deep | Stale JSDoc ("cumulative") | Update doc-comment |

**STEP 4 — Gate decision.** 0 P1, 1 P2 → **PROCEED with warning — user must ACK before blessing.**

**Asked the user:** *"Review found 1 P2 (float money math before the best-of comparison — could mis-rank near-ties) and 1 P3 (stale JSDoc). ACK to bless as-is, or fix now before push?"*

---

### Turn 5 — user

> Fix the P2 now — don't ACK it away. Take the P3 with it.

Routing the findings through the fix cycle (review STEP 4/5 contract: findings route to `/dev-pipeline:fix`, then re-review the new HEAD).

→ Invoking command: **`/dev-pipeline:fix`** (findings file `.claude/review-findings-3f7a2c1.md` as intake)

- **Step 1.5 triage:** both findings are **technical/mechanical** — the correct behaviour is already locked; only the mechanism (integer cents) is in question → spec-elicitor SKIPPED, straight to Step 2. No second Socratic pass (Rule 23: self-evident → skip).
- **Step 2:** tests first — tightened the tie-break spec to amounts that differ only below one cent in float math (fails against the float implementation, proving the P2 is real). Then:
  → `Edit apps/api/src/modules/pricing/discount.service.ts` — candidates computed via `percentOfCents(subtotalCents, LOYALTY_PCT[tier])` / promo in cents; `pickBestDiscount` compares integers; JSDoc updated.
  → Spawning Agent: **`validator`** → lint PASS · tsc PASS · unit 215 PASS · e2e 31 PASS · build PASS — **Overall: PASS**
- **Step 3:** staged the two files + execution-doc addendum, → Invoking Skill: **`commit-commands:commit`**
  ```
  fix(api): compare discount candidates in integer cents (review P2) + JSDoc

  - Review finding #1 (P2): float dollar math could mis-rank near-tie candidates and
    violate the locked "tie → promo" rule — candidates now computed/compared in cents
  - Review finding #2 (P3): stale "cumulative" JSDoc corrected
  [fix/loyalty-promo-discount-stacking 8d04b9e]
  ```

**post-commit hook fires again** — new commit invalidated the pending blessing state (Rule 8: *every new commit invalidates the blessing*):
```
🔍 AUTO-REVIEW DIRECTIVE — commit 8d04b9e… → Run /dev-pipeline:review before any further work.
```

→ Invoking command: **`/dev-pipeline:review`** (round 2, on new HEAD `8d04b9e`)
STEP 1 diff `a91c2e4..8d04b9e` · STEP 1.5 no categories fire, sidecar `knowledge-refs-8d04b9e.json` emitted (empty array) · STEP 1.6 verify-sdk-surface → skips (workspace-internal only) · STEP 2 all seven reviewers re-run in parallel → **0 P1 · 0 P2 · 0 P3** (both prior findings verified fixed) · STEP 3 findings file saved · STEP 4 gate: bless immediately.

**STEP 5 — Bless:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ REVIEW PASSED — HEAD blessed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SHA:      8d04b9e
Files:    5
Findings: 0 P1, 0 P2, 0 P3
Blessed:  .claude/.last-reviewed-sha
Next:     git push   OR   /dev-pipeline:deliver
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
**STEP 6:** appended `{"event":"review.complete","sha":"8d04b9e","p1":0,"p2":0,"p3":0,"blessed":true,"ts":"2026-07-06T09:41:22Z"}` to `.claude/agent-events.jsonl`.

**Fix flow OUTPUT:** issue #1 (loyalty/promo stacking) fixed + 2 review findings fixed · files: `discount.service.ts`, `discount.service.spec.ts`, `tests/e2e/booking/checkout.spec.ts`, `docs/discount-composition/discount-composition-execution.md` · review status: clean, HEAD blessed.

**Phase-transition check (Rule 3):** all MIUs for this task complete, no PR exists → *"All MIUs complete. Proceeding to delivery gate."* (Rule 4: never improvise delivery.)

→ Invoking command: **`/dev-pipeline:deliver`**

**Phase 8.5 precondition:** `.claude/.last-reviewed-sha` == HEAD (`8d04b9e`) — blessing fresh, no re-review needed.

**PHASE 9 — Commit & PR.**
→ Spawning Agent: **`doc-writer`** — confirmed `docs/discount-composition/discount-composition-execution.md` covers MIU 1 + the review-fix addendum; refreshed `.claude/pipeline-state.json` pointer (`phase: deliver`).
Working tree clean (both commits already made via `/commit`).
→ Invoking Skill: **`commit-commands:commit-push-pr`** — push branch + open PR.

**pre-push hook fires:**
```
✅ pre-push Gate A: review blessing matches HEAD    (8d04b9e == .claude/.last-reviewed-sha)
✅ pre-push Gate B: doc-update guard passed         (docs/discount-composition/*.md in push range)
```
PR **#104** created — "fix(api): checkout applies best-of discount — loyalty and promo no longer stack". Body: summary, test evidence, and the 🔒 Intent Lock block verbatim (Rule 23: the lock travels in the PR body).

**PHASE 9.5 — Conflict Gate (mandatory, no override).**
```
$ gh pr view 104 --json mergeable,mergeStateStatus → "MERGEABLE|CLEAN"
✅ PR 104 mergeable (state: MERGEABLE|CLEAN) — proceeding to review.
```

**PHASE 10 — Code Review.** → Delegating to **`/code-review`** on PR #104 → clean (same blessed diff; no new findings). No auto-merge yet.

**PHASE 10.5 — Browser E2E Gate.** Surface detection: changed files are `apps/api/src/**`, `tests/e2e/**`, `docs/**` — no `apps/*/(src|messages|public)` under a Next config, no `packages/ui/**`:
```
ℹ️  No frontend files touched — skipping E2E gate.
    (Docs-only / config-only / backend-only diffs skip Playwright.)
```
Rule 6 remains satisfied: the payment-touching flow's E2E (checkout best-of spec) exists and passed in-process during validation (logged exit 0), not deferred.
```
$ gh pr merge 104 --squash --auto
✓ Pull request #104 will be automatically merged via squash when requirements are met
```

**PHASE 11 — Monitor Merge & CI.**
```
$ gh run watch 9174483321 --exit-status   → ✓ CI passed (lint, tsc, unit, api-docker-build)
```
Merged to main as squash commit `c31e0f5`. Pointer rotated: `.claude/pipeline-state.json → .claude/archive/pipeline-state-104.json` (merged PR's pointer is spent; durable record is the tracked execution doc).

**PHASE 12 — Smart Deploy.** → Invoking command: **`/dev-pipeline:deploy`** — `.claude/project-context.json`: WEB monorepo. Adapters: API container adapter builds + deploys the NestJS image (the touched surface); Vercel adapter redeploys unchanged frontends as no-ops. Per-adapter smoke: API `/health` 200; pricing quote endpoint returns best-of totals on a staging tenant.

**PHASE 12.5 — Production Smoke Test.** Backend-only diff → skipped automatically (same detection as PHASE 10.5's override: smoke specs would only re-test unchanged frontend surfaces). Deploy-adapter smoke above already exercised the changed API surface post-deploy.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ SHIPPED: best-of discount composition (loyalty/promo no longer stack)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PR:               …/pull/104 (squash-merged → c31e0f5)
Preview E2E:      skipped — backend-only (Phase-8 e2e: 31/31 passed, incl. checkout best-of)
CI:               ✅ passed
Deploy:           ✅ API container deployed + smoke green
Production smoke: skipped — backend-only (adapter smoke on changed surface: ✅)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Post-delivery housekeeping:**
- → Invoking command: **`/dev-pipeline:sync`** — project-sync updated `PROJECT_STATUS.md` (no active task) and `RECENT_CHANGES.md` (entry for PR #104 with files + Intent Lock summary).
- Rule 9 trigger 2 (post-delivery one-shot, offer only): *"Want a `/dev-pipeline:refactor` proposal pass on the pricing module while it's fresh? Propose-only — nothing applied without your acceptance."* — **User: "Not now."** → not run.
- `.learnings/JOURNAL.md` holds both `fix(` entries; the scheduled `/dev-pipeline:consolidate-lessons` sweep will fold them into engineering-craft.

---

## Gates fired (summary)

| Gate | Where | Outcome |
|---|---|---|
| Routing (Rule 1) | classification announced → `/dev-pipeline:fix` | BUG_FIX_SIMPLE (business/behavioural) |
| Socratic gate (Rule 23 / fix Step 1.5) | spec-elicitor Mode B, 3 Qs | 🔒 Intent Lock (best-of, tie→promo, platform-wide) |
| Rule 11 class-level fix check | root-cause analysis | fixed at `applyDiscounts` choke point; both callers covered |
| Rule 19 old-test triage | spec change | stacking spec deleted WITH comment; invariant test added |
| Rule 6 E2E (payment-touching) | validation | checkout best-of e2e spec added + passed in-process |
| Validator gate (fix Step 2.5) | ×2 | PASS both rounds |
| assumption-checker (Rule 14; MIU boundary + review A0) | ×3 | PASS |
| AUTO-REVIEW DIRECTIVE (Rule 2, post-commit hook) | ×2 | review run immediately both times |
| Review gate (Rule 8) | round 1: 1 P2 → user chose fix; round 2: clean | HEAD 8d04b9e blessed |
| verify-sdk-surface (Rule 22) | review STEP 1.6 / validate 2.6 loose trigger | skipped cleanly — no third-party surface |
| pre-push Gate A (blessed SHA) + Gate B (doc guard) | deliver push | both ✅ |
| Conflict gate (Rule 15, PHASE 9.5) | after PR create | MERGEABLE |
| Browser E2E gate (Rule 16, PHASE 10.5) | surface detection | auto-skip (backend-only) |
| Production smoke (Rule 17, PHASE 12.5) | post-deploy | auto-skip (backend-only); adapter smoke green |
| Refactor offer (Rule 9, propose-only) | post-delivery | offered, declined |
