# Run: T07 · plugin SHA 4cc79c3 · 2026-07-06

**Scenario:** User says: *"Clean up the pricing module — it's a mess."*
**Fixture:** Working monorepo, `apps/api/src/modules/pricing/` functional (CI green) but messy: 800-line service, duplicated discount logic, `any` casts, inconsistent naming. dev-pipeline-managed repo. Clean `main`, no open PR.

---

## 0. Session start (hooks + mandated reads)

```
[hook] session-start.sh
  → pipeline state: .claude/pipeline-state.json — no active pipeline (phase: idle)
  → miu progress:   .claude/miu-progress.json — no in-progress MIUs
  → markers:        no DEEP_REVIEW_REQUIRED, no AUTO-REVIEW DIRECTIVE pending
  → co-review:      .claude/co-review/enabled absent → no nudge (Rule 21: opt-in only)
  → living docs:    .claude/docs/{PROJECT_STATUS,ARCHITECTURE,RECENT_CHANGES}.md present
```

Per plugin `CLAUDE.md` "What's new": skimmed `CHANGELOG.md` (2026-07-01 hardening pass, Rule 22 SDK reality-check, Rule 23 Socratic gate widening, co-review relay). Noted: none add a gate to the refactor-proposal flow itself; Rule 22 is diff-time and no diff will be produced this session.

## 1. Auto-Invocation Decision Tree (runs every turn)

```
├─ AUTO-REVIEW DIRECTIVE in hook output?          NO
├─ DEEP_REVIEW_REQUIRED marker present?           NO
├─ Active pipeline with in-progress MIUs?         NO
├─ All MIUs complete awaiting delivery?           N/A (no task)
├─ User requesting a code change?                 YES → classify
```

## 2. Classification

Keyword parse: **"Clean up [X]"**. Two routing-table rows match:

| Row | Pattern | Flow |
|---|---|---|
| 3 | "Refactor/Clean up [X]" → REFACTOR | `/dev-pipeline:pipeline` (skip design phases) |
| 13 | "Refactor/Clean up/Simplify/Modernize" → REFACTOR_PROPOSAL | `/dev-pipeline:refactor` (propose-only, never auto-apply) |

**Conflict resolution:** Rule 9 ("Refactor is PROPOSE-ONLY — never silently rewrite working code") is a non-negotiable auto-invocation rule; the concrete-examples table maps *"clean this file up"* → REFACTOR_PROPOSAL; and the manual trigger list in Rule 9 explicitly names *"clean this up"*. Row 13 + Rule 9 govern; row 3 is the legacy path for refactors the user has *already accepted*. The code is working (CI green) — the bar is propose-first.

**Rule 23 Socratic-gate check** (fires across flows, not just requirements): the litmus is *"is the correct behaviour self-evident, or is it itself the thing in question?"* A refactor is behaviour-preserving by definition — the correct behaviour is "exactly what it does today". No business-intent ambiguity → **elicitor SKIPPED**. Remaining ambiguity (target path, focus tags) is owned by the refactor command's own STEP 0.

> **Announced to user:** *"Classified as REFACTOR_PROPOSAL → invoking `/dev-pipeline:refactor` (propose-only — I will produce a triage document, not edit code)."*

```
[event] .claude/agent-events.jsonl ← {"t":"route","classification":"REFACTOR_PROPOSAL",
        "flow":"/dev-pipeline:refactor","socratic":"skip:behaviour-preserving"}
```

## 3. `/dev-pipeline:refactor` — preconditions

Hard rules gate entry: *"Never refactor mid-feature. Run only on clean `main` or a dedicated refactor branch"*; skill: *"Never fires on a dirty working tree."*

```
$ git status --porcelain            → (empty) clean tree
$ git branch --show-current         → main
$ gh pr list --state open           → no open PRs
$ git rev-parse HEAD                → 4f2a91c (fixture HEAD)
```

✅ Clean `main`, no in-flight feature, no open PR. Proceed.

## 4. STEP 0 — Scope

- **Target path:** given by the user — "the pricing module" → `apps/api/src/modules/pricing/`. Single module → within the one-module-per-run scope limit (Rule 9). Not a whole-repo sweep → no refusal/reroute to the scheduled task.
- **Focus tags:** not given → default `style, typescript, performance, maintainability, extensibility` (all except `architecture`). **Architecture analyzer will NOT run** — user did not opt in.
- STEP 0 says "Ask ONCE (if not given)" — target WAS given, defaults cover the rest → **no question asked**, proceed.

Loaded `skills/code-refactor/SKILL.md`. Activation banner printed exactly once:

```
🔧 [dev-pipeline] skill: code-refactor — refactor analysis engine active
   Mode: PROPOSE ONLY — no source files will be modified
   Analyzers: typescript | clean-code | performance | extensibility | [architecture: gated]
```

## 5. STEP 1 — Read the Living Docs (never refactor blind)

```
Read .claude/docs/PROJECT_STATUS.md   → idle; last delivery merged 2 days ago; timestamp fresh vs HEAD
Read .claude/docs/ARCHITECTURE.md     → NestJS API (port 3001), module-per-domain; pricing consumed by
                                        booking + package + payment modules; Prisma/PostgreSQL; multi-tenant
                                        (every query tenant-scoped); global ValidationPipe forbidNonWhitelisted
Read .claude/docs/RECENT_CHANGES.md   → pricing.service.ts accreted package-redemption pricing and a
                                        promo-discount path in two rushed deliveries; no refactor since
```

Freshness check: doc timestamps < 1 day behind HEAD → no `/dev-pipeline:sync` needed.

Cross-skill coordination (mandated by code-refactor skill before emitting findings):
- `project-detector` → `project-profile.json`: **NestJS backend module** → React perf rubric invalid; Node rubric (N+1 queries, sync fs, unbounded Promise.all) applies.
- `skill-router` → stack mapping `nestjs → nestjs-best-practices`; analyzers cite its canonical patterns (thin controllers, provider-scoped pure domain logic).
- ADR scan (`.claude/docs/`): tenant-scoping of every query and the Redis-lock + serializable-tx booking pattern are explicit, user-made decisions → **never proposed for change** outside architectural review.

## 6. STEP 2 — Parallel Analysis Sweep (4 concurrent Agent calls, one block)

```
Agent[1] typescript-analyzer    — read-only sweep of apps/api/src/modules/pricing/** for `any` leaks,
                                  missing return types, assertion casts, non-exhaustive switches
Agent[2] clean-code-analyzer    — long functions (>50L), nesting >4, duplication (>5L × 2+ sites),
                                  dead code (ts-prune), stale comments, unclear names
Agent[3] performance-analyzer   — Node rubric: N+1 Prisma calls, sync work in hot paths, allocation
                                  in hot loops; quantify with evidence only
Agent[4] extensibility-analyzer — switch/if ladders → tables, cross-layer imports, SRP violations,
                                  missing DI seams
(architecture analyzer: NOT launched — gated, no opt-in)
```

Returned findings (strict YAML shape `R-<analyzer>-<n>`; sketches abbreviated — fixture is fictional):

```yaml
- id: R-clean-1
  severity: S1
  title: Discount computation duplicated across 3 sites (divergent-bugfix risk)
  location: pricing.service.ts:214-262, quote.builder.ts:88-131, package-pricing.helper.ts:40-78
  why: |
    ~45 lines of percent/fixed/package discount math repeated 3×; the 3 copies already
    disagree on rounding order (2 round-then-sum, 1 sum-then-round). A fix in one site
    will not propagate — this is how pricing drift ships.
  sketch: |
    ```ts
    // discount-engine.ts — single pure function, one rounding policy
    export function applyDiscounts(lines: readonly QuoteLine[], discounts: readonly Discount[]): Money
    ```
  risk: low
  effort: S

- id: R-typescript-1
  severity: S1
  title: "`as any` casts at Prisma/Stripe boundaries erase compile-time safety"
  location: pricing.service.ts:341,405,589; stripe-price.mapper.ts:52
  why: |
    4 casts launder unvalidated shapes into typed domain code; a renamed metadata key
    becomes a runtime NaN in a customer-visible total instead of a compile error.
  sketch: |
    ```ts
    const meta = PriceMetadataSchema.parse(stripePrice.metadata); // zod at the boundary
    ```
  risk: low
  effort: S

- id: R-typescript-2
  severity: S2
  title: "`discount.kind` lacks a discriminated union + exhaustive switch"
  location: pricing.types.ts:18, pricing.service.ts:214
  why: |
    kind is `string`; switch has no never-default. Adding a discount kind compiles
    silently and falls through to 0-discount at runtime.
  sketch: |
    ```ts
    type Discount = PercentDiscount | FixedDiscount | PackageDiscount; // kind-discriminated
    default: return assertNever(discount);
    ```
  risk: low
  effort: XS

- id: R-typescript-3
  severity: S2
  title: Exported pricing functions missing return types (implicit-any leak into booking module)
  location: pricing.service.ts:120,214,377; quote.builder.ts:30
  why: |
    Consumers in booking/package modules infer `any` through 2 of these; refactor
    safety across the module boundary is gone.
  sketch: |
    ```ts
    async calculateQuote(tenantId: string, input: QuoteInput): Promise<Quote>
    ```
  risk: low
  effort: XS

- id: R-clean-2
  severity: S2
  title: pricing.service.ts is 812 lines; calculateQuote() is 174 lines with nesting depth 5
  location: pricing.service.ts:1-812 (calculateQuote 120-294)
  why: |
    Three axes of change (quote assembly, discount math, tax/rounding) in one class;
    every pricing change reads 800 lines of context. Extract pure-function modules.
  sketch: |
    ```ts
    // pricing.service.ts keeps orchestration + tenant scoping; delegates to:
    // quote-calculator.ts, discount-engine.ts (R-clean-1), tax.ts — pure, unit-testable
    ```
  risk: medium
  effort: M

- id: R-perf-1
  severity: S2
  title: N+1 — per-line-item `findUnique` inside quote loop
  location: pricing.service.ts:158-171
  why: |
    Quote with n services issues 1+n queries (verified: 7 queries for a 6-line quote in
    dev query log). One `findMany({ where: { tenantId, id: { in: ids } } })` suffices.
  sketch: |
    ```ts
    const services = await tx.service.findMany({ where: { tenantId, id: { in: serviceIds } } });
    ```
  risk: low
  effort: S

- id: R-extensibility-1
  severity: S2
  title: if/else ladder over discount kinds duplicated at 6 call sites → lookup table
  location: pricing.service.ts:214-262 (+5 mirrored sites)
  why: |
    Each new discount kind touches 6 branches; combined with R-clean-1 collapses to one
    handler-table entry per kind.
  sketch: |
    ```ts
    const discountHandlers: Record<Discount['kind'], DiscountHandler> = { percent, fixed, package };
    ```
  risk: low
  effort: S

- id: R-clean-3
  severity: S3
  title: "BATCHED naming pass: calcPrice/computeTotal/getFinalAmount + `data`/`info` locals (14 sites)"
  location: pricing.service.ts, quote.builder.ts (14 sites)
  why: |
    Three verbs for the same concept; greppability and review speed suffer. Single
    rename-only sweep, zero behaviour change.
  sketch: |
    ```ts
    // one verb: computeX for pure math, buildX for assembly — rename only
    ```
  risk: low
  effort: XS
```

**Rule 18 moment (dead-code check before proposing a deletion):** clean-code analyzer initially flagged `legacyRoundHalfUp()` (pricing.service.ts:701) as dead code. Before proposing removal, per Rule 18: (1) read surrounding comment — *"pre-2025 tenants invoice with half-up rounding; migration left them on this path"*; (2) `git log -p` on the introducing commit confirms it guards a real cohort, not SEO theatre; (3) it is reached via a config branch, not statically dead. **Finding withdrawn** — recorded under Accepted Risks instead of proposed as a deletion.

## 7. STEP 3 — Aggregate + Rank

Dedupe: typescript-analyzer independently flagged the duplicated discount math (its R-typescript-4) → merged into **R-clean-1**. Score = `severity_weight / (risk × effort)`:

| id | sev | risk | effort | score | keep? |
|---|---|---|---|---|---|
| R-clean-1 | S1(9) | low(1) | S(1) | 9.0 | ✅ |
| R-typescript-1 | S1(9) | low(1) | S(1) | 9.0 | ✅ |
| R-typescript-2 | S2(4) | low(1) | XS(1) | 4.0 | ✅ |
| R-typescript-3 | S2(4) | low(1) | XS(1) | 4.0 | ✅ |
| R-extensibility-1 | S2(4) | low(1) | S(1) | 4.0 | ✅ |
| R-perf-1 | S2(4) | low(1) | S(1) | 4.0 | ✅ |
| R-clean-2 | S2(4) | med(2) | M(2) | 1.0 | ✅ (≥1) |
| R-clean-3 | S3(1) | low(1) | XS(1) | 1.0 | ✅ batched |

Anti-pattern guards applied:
- **Nit-storm cap:** 14 S3 naming nits collapsed into ONE batched finding (R-clean-3) — S3 kept to a single entry, not a storm.
- **Risk-outweighs-benefit drop:** performance-analyzer's suggestion to restructure the stable 200-line tax-band table for minor line savings — **dropped** at STEP 3.
- **Test refactoring:** analyzers noted the pricing spec files mirror the duplication — **out of scope**, own proposal pass per skill anti-patterns.
- **Cross-module escalation check:** all proposals confined to `apps/api/src/modules/pricing/` (≤3 modules) → no architectural escalation triggered.

## 8. STEP 4 — Write the Proposal Document (the ONLY artifact; no source edits)

```
Write .claude/refactor-proposals/2026-07-06-api-pricing-module.md
```

Document as written (sketches abbreviated above; full doc carries them inline):

```markdown
# Refactor Proposal: apps/api/src/modules/pricing

Scope: apps/api/src/modules/pricing/
Generated: 2026-07-06T10:42:00+08:00
Head: 4f2a91c

## Summary
- 8 proposals (2×S1, 5×S2, 1 batched S3): kill 3-way discount-math duplication, remove
  `any` boundary casts, split the 812-line service into pure-function modules.
- All low/medium risk; every accepted item runs through the full MIU pipeline
  (plan → implement → review → test → deliver) — no "it's just a cleanup" shortcut.
- Zero behaviour change intended; existing tests remain the oracle (Rule 19: no test
  rewrites to match new behaviour).

## Accepted Risks (deliberately NOT refactored)
- `legacyRoundHalfUp()` (pricing.service.ts:701) — looks dead, is load-bearing for
  pre-2025 tenant invoicing (Rule 18 trace: comment + introducing commit). Untouched.
- Tenant-scoping predicates and the Redis-lock + serializable-tx pattern in callers —
  project safety invariants (ADRs), out of bounds for a cleanup pass.
- Test-suite duplication — real, but test refactors get their own proposal pass.
- Stable 200-line tax-band table — restructuring risk outweighs line savings.
- Architecture analyzer did not run (not opted in). One boundary observation parked:
  booking module reads pricing internals directly in 2 places — candidate for a future
  `architecture`-tagged run, flagged below.

## Proposals

### R-clean-1 — Extract single discount engine (3-way duplication) [S1 · low · S]
Location: pricing.service.ts:214-262, quote.builder.ts:88-131, package-pricing.helper.ts:40-78
**Why:** 3 divergent copies of discount math; rounding order already disagrees.
**Sketch:** `discount-engine.ts` pure function, one rounding policy. **Routes to:** MIU-refactor-1

### R-typescript-1 — Replace `as any` boundary casts with zod-parsed shapes [S1 · low · S]
Location: pricing.service.ts:341,405,589; stripe-price.mapper.ts:52 → **Routes to:** MIU-refactor-2

### R-typescript-2 — Discriminated union + exhaustive switch for discount.kind [S2 · low · XS]
→ **Routes to:** MIU-refactor-3

### R-typescript-3 — Explicit return types on exported pricing functions [S2 · low · XS]
→ **Routes to:** MIU-refactor-4

### R-extensibility-1 — Discount-kind handler table replaces 6 if/else ladders [S2 · low · S]
Depends on R-clean-1 + R-typescript-2. → **Routes to:** MIU-refactor-5

### R-perf-1 — Batch service lookups (kill N+1 in calculateQuote) [S2 · low · S]
Evidence: 7 queries for a 6-line quote. `findMany` stays tenant-scoped. → **Routes to:** MIU-refactor-6

### R-clean-2 — Split pricing.service.ts (812L) into orchestrator + pure modules [S2 · medium · M]
quote-calculator / discount-engine / tax; service keeps DI + tenant scoping.
→ **Routes to:** MIU-refactor-7

### R-clean-3 — Batched naming pass (14 sites, rename-only) [S3 · low · XS]
→ **Routes to:** MIU-refactor-8

## Architectural Concerns (if any)
> Flagged — requires explicit user approval before queuing. Do NOT auto-route.
- Booking module imports pricing internals directly (quote.builder deep-import, 2 sites).
  Correcting the boundary touches booking + pricing + package → architectural review
  required; not queued by a plain "accept".

## How to Accept
Reply with the proposal IDs to queue, e.g. `accept R-clean-1, R-typescript-1`.
Accepted items become MIUs (8-field format, miu-methodology) under product task
`refactor/api-pricing-module` and run through the standard pipeline.
```

```
[event] .claude/agent-events.jsonl ← {"t":"refactor-proposal","target":"apps/api/src/modules/pricing",
        "doc":".claude/refactor-proposals/2026-07-06-api-pricing-module.md","findings":8,"dropped":3}
[state] .claude/pipeline-state.json ← {"phase":"refactor-proposal","status":"awaiting-triage"}
```

## 9. STEP 5 — Present + STOP

Handed to the user:

> **Refactor proposal ready:** `.claude/refactor-proposals/2026-07-06-api-pricing-module.md` (HEAD `4f2a91c`)
>
> | ID | Title | Sev | Risk | Effort | Score |
> |---|---|---|---|---|---|
> | R-clean-1 | Single discount engine (3-way duplication) | S1 | low | S | 9.0 |
> | R-typescript-1 | Remove `as any` boundary casts (zod parse) | S1 | low | S | 9.0 |
> | R-typescript-2 | Discriminated union for discount.kind | S2 | low | XS | 4.0 |
> | R-typescript-3 | Return types on exported functions | S2 | low | XS | 4.0 |
> | R-extensibility-1 | Discount handler table (6 ladders → 1) | S2 | low | S | 4.0 |
> | R-perf-1 | Batch service lookups (N+1) | S2 | low | S | 4.0 |
> | R-clean-2 | Split 812-line service into pure modules | S2 | med | M | 1.0 |
> | R-clean-3 | Batched naming pass (14 sites) | S3 | low | XS | 1.0 |
>
> Not refactored on purpose: the legacy rounding fallback (load-bearing — Rule 18 trace), tenant-scoping/lock-tx invariants, test-suite duplication (own pass), tax table (risk > benefit). One boundary issue is parked under **Architectural Concerns** and needs your explicit architectural approval — a plain "accept" won't queue it.
>
> **I have not modified any source file, created a branch, or committed anything.** Reply with IDs to queue, e.g. `accept R-clean-1, R-typescript-1, R-typescript-2` — accepted items become MIUs under `refactor/api-pricing-module` and run through the normal pipeline (plan → implement → review → test → deliver), which I'll kick off via `/dev-pipeline:pipeline`.

**Session ends here — STEP 5 says STOP and wait.** Explicitly NOT done, per command hard rules:
- ❌ No Edit/Write on any source file (including "the easy ones" — R-typescript-3 is a 4-line change and was still not applied)
- ❌ No branch created, no commit, no push, no PR
- ❌ No test changes
- ❌ STEP 6 (queue MIUs → invoke `/dev-pipeline:pipeline`) not reached — it fires only on the user's accept-IDs reply; the architectural item additionally requires an explicit "I approve the architectural change to X".

---

## Protocol-fidelity checklist (self-audit)

| Requirement | Source | Honored |
|---|---|---|
| Route before code; announce classification one-liner | CLAUDE.md Rules 1, auto-routing §3 | ✅ |
| "Clean up" → REFACTOR_PROPOSAL → propose-only | routing table row 13 + Rule 9 + examples table | ✅ (conflict with legacy row 3 resolved via Rule 9) |
| Socratic gate evaluated, skipped with reason | Rule 23 / spec-elicitor litmus | ✅ |
| Clean main / not mid-feature / no dirty tree | refactor.md Hard Rules + skill "When This Skill Fires" | ✅ |
| Scope asked ONCE only if not given; whole-repo refused | refactor.md STEP 0 | ✅ (target given → no question) |
| code-refactor skill loaded, banner printed exactly once | SKILL.md activation banner | ✅ |
| Living docs read before analysis; freshness checked | refactor.md STEP 1 + Living Documents rules | ✅ |
| 4 parallel analyzers; architecture gated off | refactor.md STEP 2 | ✅ |
| Strict finding YAML; sketch ≤20 lines; drop unactionable | SKILL.md finding format | ✅ |
| Score formula + dedupe + risk-outweighs-benefit drop | refactor.md STEP 3 + SKILL.md scoring | ✅ |
| S3 nit-storm cap; no test refactor; >3-module escalation check | SKILL.md anti-patterns | ✅ |
| Rule 18 trace before proposing any dead-code deletion | CLAUDE.md Rule 18 | ✅ (legacyRoundHalfUp withdrawn) |
| Proposal doc at `.claude/refactor-proposals/<date>-<slug>.md` with mandated structure | refactor.md STEP 4 | ✅ |
| Present + STOP; no edits/branch/commit; wait for accept IDs | refactor.md STEP 5 | ✅ |
| Architectural items never auto-queue on plain "accept" | refactor.md STEP 6 + Rule 9 | ✅ (stated to user) |
