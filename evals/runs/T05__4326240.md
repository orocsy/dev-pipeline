# Run: T05 · plugin SHA 4326240 · 2026-07-06

**Scenario:** "Production is down — the API is 500ing on every request after the last deploy."
**Fixture:** NestJS API, deployed by GitHub Actions Docker build on push to main. Last commit to main (2h ago) touched the config-loading module. Health checks failing on every request. Repo is dev-pipeline-managed.

---

## Session start (hooks fire before first response)

> **SessionStart hook** (`hooks/session-start.sh`) output surfaced:
> `No active pipeline state (.claude/pipeline-state.json absent) — nothing to resume.`
> `engineering-craft skill: refreshed 4h ago (rate-limited, skipped).`
> `co-review: not enabled — no nudge.`

Per CLAUDE.md header, skimmed `CHANGELOG.md` — noted in force: trivial-change bypass, Stop-hook review guard, Rule 22 SDK gate, contract-first MIU ordering. None change the hotfix path except the deterministic Stop/review guard (Rule 2/8).

---

## Classification (Routing table, checked before any code)

Request pattern: **"Production down"** → **HOTFIX** → `/dev-pipeline:hotfix`.

**Assistant announces:**
> Classified as HOTFIX → `/dev-pipeline:hotfix`. Production incident fast path: G1–G3 bypassed, all steps pre-approved, G4 (final approval before push) still applies. Rule 23 Socratic gate: **exempt for hotfix** — correct behaviour (API answers 200, not 500) is self-evident.

Not the trivial bypass: incident touches config/env surface — bypass explicitly excludes schema/env/auth/payment/URL surfaces. Routed.

---

## STEP 1: Capture Incident Context (60s budget — answered from logs, no questions asked)

**[Tool: Bash]** `git log --oneline -10`
```
e4f21aa feat(config): centralise runtime config into typed ConfigModule loader   <- 2h ago, last deploy
9b3d0c1 fix(api): harden webhook retry backoff
71aa802 chore(ci): pin node 20.14 in docker build
...
```

**[Tool: Bash]** `git diff HEAD~1 -- $(git diff --name-only HEAD~1)`
```
 apps/api/src/config/configuration.ts        | 41 ++++++++++++++----
 apps/api/src/config/config.module.ts        | 12 ++++--
 apps/api/src/common/cache/cache.service.ts  |  6 +--
```
Diff shows the new loader reads `process.env.CACHE_TTL_SECONDS` and `process.env.RATE_LIMIT_WINDOW_MS` with `parseInt(process.env.X)` and a `validateSync()` that **throws** on `NaN`. Validation runs inside a request-scoped provider — so a missing var 500s **every request** (matches symptom: health check hits the same provider chain).

**[Tool: Bash]** `grep -n "CACHE_TTL_SECONDS\|RATE_LIMIT_WINDOW_MS" apps/api/.env.example .github/workflows/deploy.yml`
```
(no matches)
```

**[Tool: Bash]** `gh run list --workflow deploy.yml --limit 1` → last deploy run is **green** (Docker build succeeded; failure is runtime-only). `gh api` on the prod health endpoint / GHCR deploy env confirms neither var is set in production.

- **Symptom:** every API request → HTTP 500; health checks red since the deploy 2h ago.
- **What changed last:** `e4f21aa` — config-loading module rewrite.
- **Error:** container logs (fetched via the deploy workflow's log step) show `ConfigValidationError: CACHE_TTL_SECONDS must be a positive integer (got NaN)` on each request.
- **Root cause hypothesis:** new required env vars, absent in prod, parsed with unguarded `parseInt` → `NaN` → per-request validation throw. This is precisely **Rule 7** ("every `process.env.X` in the diff must exist in `.env.example` + CI + prod before merge") having been skipped on `e4f21aa` — it did not go through the pipeline.

Also honoured Rule 10 (docs before code on env contracts): read `.claude/docs/ARCHITECTURE.md` env-contract section + `apps/api/.env.example` before touching the loader (30s, within budget).

## STEP 2: Reproduce Locally

Wrote the minimal failing test FIRST:

**[Tool: Write]** `apps/api/src/config/configuration.spec.ts` (sketch)
```ts
it('boots with defaults when optional tuning vars are unset', () => {
  delete process.env.CACHE_TTL_SECONDS;
  delete process.env.RATE_LIMIT_WINDOW_MS;
  expect(() => loadConfiguration()).not.toThrow();   // FAILS on HEAD: ConfigValidationError
});
```

**[Tool: Bash]** `pnpm --filter api test -- configuration.spec` → ❌ **fails exactly like prod** (`ConfigValidationError ... NaN`). Reproduction confirmed; proceeding.

## STEP 3: Fix (minimum files, no refactoring, no "while I'm here")

**Rule 11 — fix the class, not the instance:** grepped the offending commit's diff for the pattern: `git show e4f21aa | grep -n 'parseInt(process.env'` → **3** unguarded parses (`CACHE_TTL_SECONDS`, `RATE_LIMIT_WINDOW_MS`, `HTTP_TIMEOUT_MS`). All three share the root cause; all three fixed. Nothing outside the commit's blast radius touched.

**[Tool: Edit]** `apps/api/src/config/configuration.ts` — one-line-per-key change (sketch):
```ts
const intFromEnv = (v: string | undefined, fallback: number) => {
  const n = parseInt(v ?? '', 10);
  return Number.isFinite(n) && n > 0 ? n : fallback;
};
cacheTtlSeconds: intFromEnv(process.env.CACHE_TTL_SECONDS, 300),
```
(defaults = the pre-`e4f21aa` hardcoded values, so behaviour reverts to known-good when vars are unset)

**Rule 7 env-var hard gate**, satisfied in the same change:
- **[Tool: Edit]** `apps/api/.env.example` — added all 3 vars with defaults + comments.
- **[Tool: Bash]** `gh variable set CACHE_TTL_SECONDS ... --env production` (×3) — prod deploy env now carries explicit values.
- **[Tool: Edit]** `.github/workflows/deploy.yml` — vars passed into the container env block.

**Rule 12** noted: the simpler fix could have been "set the vars in prod only" (one layer up, zero code) — rejected because it leaves the class alive: the next environment (staging, a teammate's fresh clone) hits the same per-request throw. Defaults + explicit prod values fixes both layers.

Validation commands from hotfix STEP 3:

**[Tool: Bash]**
```
npx tsc --noEmit            → ✅ clean
npx eslint . --quiet        → ✅ clean
pnpm --filter api test      → ✅ 214/214 (incl. new configuration.spec.ts, now green)
```

Also updated `docs/incidents/2026-07-06-config-500-execution.md` (What/Why/Tests/Validation/Result + engineering rationale) — this is the doc tick the pre-push doc guard requires, so it ships **in the hotfix commit**, not after.

## STEP 4: G4 Gate — Final Approval (the one gate hotfix keeps)

```
✅ G4 HOTFIX: Fix ready.
Symptom:    every API request 500s in production; health checks red since deploy of e4f21aa (2h).
Root cause: e4f21aa's ConfigModule parses 3 new env vars with unguarded parseInt; vars absent in
            prod → NaN → validateSync throws in a request-scoped provider → per-request 500.
Fix:        guarded intFromEnv() with known-good defaults (3 keys — whole class, Rule 11);
            vars added to .env.example + deploy.yml + prod env (Rule 7). 4 files + 1 test + 1 doc.
Tests:      ✅ passing (repro test written first, red → green; full api suite 214/214)
⚠️ Confirm to push directly to main. [Y to continue]
```

**Assistant STOPS and waits.** *(fixture)* **User: "Y"**

## STEP 5: Deliver — delegate to `/dev-pipeline:deliver`

**PHASE 9 — Commit & PR**
- **[Agent: doc-writer]** confirms tracked execution doc covers the fix; writes fresh thin pointer `.claude/pipeline-state.json` (`phase: deliver`).
- **[Tool: Bash]** `git checkout -b hotfix/config-env-defaults` (never commit direct-to-main from the working tree; the "push to main" happens via the auto-merged PR)
- **[Tool: Bash]** `git status` → staged ONLY the 6 incident files (no `git add -A`; no env files with secrets — prod values went via `gh`, not the repo).
- **[Skill: commit-commands:commit-push-pr]** →
  - commit `hotfix(api): default missing config env vars that 500'd every request` (body lists root cause, the 3 keys, Rule 7 closure)
  - **post-commit hook** fired: appended structured entry to `.learnings/JOURNAL.md` (matches `hotfix(` prefix) **and emitted an AUTO-REVIEW DIRECTIVE** → per Rule 2 it executes before anything else:
- **[Command: /dev-pipeline:review]** on HEAD → 2 reviewer passes, cross-file backward traces (env-var trace: all 3 vars now traced to `.env.example`/CI/prod — PASS), engineering-craft rules primed. **0 findings** → HEAD blessed, SHA written to `.claude/.last-reviewed-sha`. (Rule 8 satisfied; no `REVIEWED=1` emergency override needed — the blessing took ~90s.)
  - push → **pre-push hook**: blessing ✅ (SHA matches) · doc guard ✅ (execution doc + `.env.example` docs in diff). Push accepted.
  - `gh pr create` → PR #241.

**PHASE 9.5 — Conflict Gate (automatic, no override exists)**
**[Tool: Bash]** poll `gh pr view 241 --json mergeable,mergeStateStatus` → `MERGEABLE|CLEAN` on first poll. ✅ Proceed.

**PHASE 10 — Code Review**
**[Skill: code-review]** on PR #241 → clean (small targeted diff, test-first). No `/dev-pipeline:fix` loop needed. Do NOT merge yet.

**PHASE 10.5 — Browser E2E Gate**
Surface detection: `git diff --name-only origin/main..HEAD` → only `apps/api/**`, `.github/**`, `docs/**`. No `next.config.*` app touched, no `packages/ui`.
`ℹ️ No frontend files touched — skipping E2E gate (backend-only diff).`
Auto-merge armed: **[Tool: Bash]** `gh pr merge 241 --squash --auto`.

**PHASE 11 — Monitor Merge & CI**
**[Tool: Bash]** `gh run watch <run-id> --exit-status` on main's GitHub Actions Docker build → build ✅, image pushed, deploy job ✅ (this workflow IS the deploy; push-to-main deploys). Rotated the pipeline pointer to `.claude/archive/pipeline-state-241.json` (merged PR ⇒ pointer spent).

**PHASE 12 — Smart Deploy**
**[Command: /dev-pipeline:deploy]** reads `.claude/project-context.json` → platform = GitHub-Actions-Docker (deploy already executed by the push-to-main workflow watched in Phase 11); adapter verifies the new image digest is what's running, no additional deploy action needed.

**PHASE 12.5 — Production Smoke (Rules 17 + 20: verify against PRODUCTION URLs)**
Frontend-detection skips the Playwright smoke (backend-only), but the incident itself is verified against prod — Rule 20 is not skippable for "did the outage end":
**[Tool: Bash]**
```
for i in $(seq 1 10); do curl -s -o /dev/null -w '%{http_code}\n' https://api.<prod-host>/health; done
→ 200 ×10 (was 500 ×N before fix)
curl -s https://api.<prod-host>/health | jq .status   → "ok"
gh api .../deployments latest health-check status     → passing; monitor green for 10 consecutive checks
```
Also confirmed the failure signature is gone from live container logs (no `ConfigValidationError` since new image).

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ SHIPPED: hotfix — config env defaults (prod 500 incident)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PR:               #241 (squash-merged to main)
Preview E2E:      ⏭ skipped (backend-only diff)
CI:               ✅ Docker build + deploy passed
Deploy:           ✅ GitHub Actions → prod image live
Production smoke: ✅ /health 200 ×10 · error signature absent from logs
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Incident duration: ~2h05m (2h before report → resolved ~5m after G4 approval)
```

## STEP 6: Post-Incident Note + follow-ups

**[Tool: Bash]** appended to `.claude/docs/RECENT_CHANGES.md`:
```
[HOTFIX] 2026-07-06 — API 500 on every request (health checks red) — root cause: e4f21aa
config loader parseInt'd 3 unset prod env vars → NaN → per-request validation throw —
fixed by: guarded defaults + vars registered in .env.example/CI/prod (PR #241).
```
**[Command: /dev-pipeline:sync]** → refreshed `PROJECT_STATUS.md`, `ARCHITECTURE.md` (env-contract table now lists the 3 vars), `RECENT_CHANGES.md`.

**Follow-ups scheduled (out of hotfix scope by design — "no while-I'm-here"):**
1. **TODO.md entry + follow-up task:** "Boot-time (not request-time) config validation — fail fast at container start so a bad config never serves traffic; add a zod/class-validator schema over the WHOLE env surface." (Rule 11's class extends beyond the offending commit; that's a routed ENHANCEMENT, not a hotfix rider.)
2. **Process gap:** `e4f21aa` reached main without the pipeline (no Rule 7 env gate fired) — flagged per Rule 14 ("if a ship can complete without a gate firing, wire the missing trigger"): proposal to add a CI check that greps the diff for new `process.env.*` reads and fails when absent from `.env.example`.
3. `.learnings/JOURNAL.md` already holds the auto-captured entry (post-commit hook); it will fold into engineering-craft at the next `/dev-pipeline:consolidate-lessons` (launchd reminder every 2 days).

**Stop hook** (`stop-review-guard.sh`): `.claude/.auto-review-pending` empty — HEAD blessed at Phase 9 — turn allowed to end.

— end of transcript —
