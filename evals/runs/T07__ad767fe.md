# Run: T07 · plugin SHA ad767fe · 2026-07-06

**Scenario:** User says: *"Clean up the pricing module — it's a mess."*
**Fixture:** Working monorepo, `apps/api/src/modules/pricing/` functional (CI green) but messy: 800-line service, duplicated discount logic, `any` casts, inconsistent naming. dev-pipeline-managed repo. Clean `main`, no open PR.
**Plugin state:** post-diet — CLAUDE.md ~94 lines (routing table + G1–G4 + Rules 1–23 one-liners); full rule bodies in `docs/RULES.md`; real SessionStart/Stop hooks in `hooks/hooks.json`.

---

## 0. Session start (real hooks now, not prose-hopeful)

```
[hook SessionStart] hooks/session-start.sh
  → .claude/pipeline-state.json: no active pipeline (task=? branch=?) → no ACTIVE PIPELINE line emitted
  → .claude/.auto-review-pending: absent → no AUTO-REVIEW PENDING warning
  → engineering-craft skill: marker fresh (<24h) → no refresh spawned
  → .claude/co-review/enabled: absent → co-review-nudge suppressed (Rule 21 opt-in, now mechanical
    via disable-model-invocation on the co-review command)
```

Per CLAUDE.md header ("skim `CHANGELOG.md` at session start; anything there is in force"): skimmed. Top entry is the 2026-07-06 diet itself — noted the **trivial-change bypass** (new, must be evaluated before routing), the **Stop hook** (blocks turn-end on unblessed pending commits — will pass trivially this session since no commit is made), and **living-docs precedence softened** (docs first read, code ground truth for load-bearing answers). Older in-force entries (Rule 22 SDK reality-check, Rule 23 Socratic widening) checked for refactor-flow relevance: Rule 22 is diff/design-time and this session produces no diff or design; Rule 23 evaluated below.

## 1. Classification (ROUTING section — "check every turn that involves code")

Keyword parse: **"Clean up [X]"**. The dieted routing table has exactly ONE matching row — no conflict to resolve (the pre-diet table's legacy REFACTOR→pipeline row is gone):

| Request pattern | Type | Flow |
|---|---|---|
| "Clean up/Refactor/Simplify [X]" | REFACTOR_PROPOSAL | `/dev-pipeline:refactor` (propose-only) |

**Trivial-change bypass check (Rule 1, new — the one sanctioned shortcut):**
- Diff describable in one sentence? **NO** — "clean up the pricing module" spans an 800-line service, duplicated discount logic across files, `any` casts, naming.
- ≤2 files? **NO** — module-wide.
- Schema/env/auth/payment/URL surface? Pricing math is **payment-adjacent**.

Three independent disqualifiers → bypass rejected, route. (No `trivial-bypass` event logged — the event is only for taken bypasses.)

**Rule 23 Socratic-gate check** (one-liner in CLAUDE.md; litmus confirmed in `docs/RULES.md` / `skills/spec-elicitor` "When to run me": *"is the correct behaviour self-evident, or is it itself the thing in question?"*). A refactor is behaviour-preserving by definition — correct behaviour is "exactly what the module does today", CI green is the oracle. No business-intent ambiguity → **spec-elicitor SKIPPED** (neither Mode A nor Mode B). Residual scope ambiguity (focus tags) belongs to the refactor command's own STEP 0, not the elicitor.

**Rule 9 body consulted** in `docs/RULES.md` (the one-liner points there): propose-only, findings grouped by severity with rewrite sketches, acceptance by ID, accepted items become MIUs through the normal pipeline, architecture-level items flagged "Requires Architectural Review" and never auto-routed, ONE module per run, manual trigger list explicitly names *"clean this up"*.

> **Announced to user (one line, per ROUTING):** *"Classified as REFACTOR_PROPOSAL → `/dev-pipeline:refactor` (propose-only — I will produce a triage document, not edit code)."*

```
[event] .claude/agent-events.jsonl ← {"t":"route","classification":"REFACTOR_PROPOSAL",
        "flow":"/dev-pipeline:refactor","bypass":"rejected:3-disqualifiers",
        "socratic":"skip:behaviour-preserving"}
```

## 2. `/dev-pipeline:refactor` — entry preconditions

Command header: *"You are producing a refactor proposal, not a patch. Do NOT edit source files in this command."* Hard rules: *"Never refactor mid-feature. Run only on clean `main` or a dedicated refactor branch."* Skill: *"Never fires on a dirty working tree."*

```
$ git status --porcelain            → (empty) clean tree
$ git branch --show-current         → main
$ gh pr list --state open           → no open PRs
$ git rev-parse HEAD                → 4f2a91c (fixture HEAD)
```

✅ Clean `main`, no in-flight feature, no open PR. G1–G4 note: the pipeline gates do not apply here — refactor.md says *"All steps are pre-approved. Run to completion"*; the user gate in THIS flow is STEP 5's stop-and-wait-for-accept-IDs.

## 3. STEP 0 — Scope

- **Target path:** given by the user — "the pricing module" → `apps/api/src/modules/pricing/`. Single module → within Rule 9's one-module-per-run limit. Not a whole-repo sweep → no refusal / reroute to the scheduled refactor task.
- **Focus tags:** not given → STEP 0's stated default applies: `style, typescript, performance, maintainability, extensibility` (all except `architecture`). **Architecture analyzer will NOT run** — user did not opt in.
- STEP 0 says "Ask ONCE (**if not given**)" — target WAS given and the tags have a defined default → **no question asked**, run to completion.

Loaded `skills/code-refactor/SKILL.md`. Activation banner printed exactly once:

```
🔧 [dev-pipeline] skill: code-refactor — refactor analysis engine active
   Mode: PROPOSE ONLY — no source files will be modified
   Analyzers: typescript | clean-code | performance | extensibility | [architecture: gated]
```

## 4. STEP 1 — Read the Living Docs (never refactor blind)

```
Read .claude/docs/ARCHITECTURE.md     → NestJS API, module-per-domain; pricing consumed by booking +
                                        package + payment modules; Prisma/PostgreSQL; multi-tenant —
                                        every query tenant-scoped (explicit ADR); global ValidationPipe
                                        forbidNonWhitelisted
Read .claude/docs/RECENT_CHANGES.md   → pricing.service.ts accreted package-redemption pricing and a
                                        promo-discount path across two rushed deliveries; no refactor since
```

**Softened precedence applied (2026-07-06 changelog entry):** docs are the first read, but the analysis about to run is load-bearing — analyzers therefore verify every claim against the CODE (docs prime, code decides). Doc timestamps < 1 day behind HEAD → no `/dev-pipeline:sync` needed.

Cross-skill coordination (mandated by code-refactor SKILL.md before emitting findings):
- `skills/project-detector` → **NestJS backend module**: React perf rubric invalid; Node rubric (N+1 queries, sync work in hot paths, allocation in hot loops) applies.
- `skills/skill-router` → stack mapping `nestjs → nestjs-best-practices`; analyzers cite its canonical patterns (thin controllers, pure domain logic behind providers).
- ADR scan (`.claude/docs/`): tenant-scoping of every query and the Redis-lock + serializable-tx pattern in booking-side callers are explicit user decisions → **never proposed for reversal** outside architectural review.

## 5. STEP 2 — Parallel Analysis Sweep (4 concurrent Agent calls, single block)

```
Agent[1] typescript-analyzer    — read-only: `any` leaks, missing return types on exports, assertion
                                  casts, non-discriminated kind fields, non-exhaustive switches
Agent[2] clean-code-analyzer    — functions >50L, nesting >4, duplication (>5L × 2+ sites), dead code
                                  (ts-prune), stale comments, unclear names
Agent[3] performance-analyzer   — Node rubric: N+1 Prisma calls, sync hot-path work; evidence-only
                                  (no perf proposal without measurement)
Agent[4] extensibility-analyzer — switch/if ladders → tables, cross-layer imports, SRP violations,
                                  missing DI seams; YAGNI hard default on speculative S3s
(architecture analyzer: NOT launched — gated, no opt-in)
```

Returned findings (strict YAML shape `R-<analyzer>-<n>`; sketches abbreviated — fixture is fictional):

```yaml
- id: R-clean-1
  severity: S1
  title: Discount computation duplicated across 3 sites (divergent-bugfix risk)
  location: pricing.service.ts:214-262, quote.builder.ts:88-131, package-pricing.helper.ts:40-78
  why: |
    ~45 lines of percent/fixed/package discount math repeated 3×; the copies already
    disagree on rounding order (2 round-then-sum, 1 sum-then-round). A fix in one
    site will not propagate — this is how pricing drift ships.
  sketch: |
    ```ts
    // discount-engine.ts — single pure function, one rounding policy
    export function applyDiscounts(lines: readonly QuoteLine[], discounts: readonly Discount[]): Money
    ```
  risk: low
  effort: S

- id: R-typescript-1
  severity: S1
  title: "`as any` casts at Prisma/Stripe boundaries erase compile-time safety"
  location: pricing.service.ts:341,405,589; stripe-price.mapper.ts:52
  why: |
    4 casts launder unvalidated shapes into typed domain code; a renamed metadata key
    becomes a runtime NaN in a customer-visible total instead of a compile error.
  sketch: |
    ```ts
    const meta = PriceMetadataSchema.parse(stripePrice.metadata); // zod at the boundary
    ```
  risk: low
  effort: S

- id: R-typescript-2
  severity: S2
  title: "`discount.kind` lacks a discriminated union + exhaustive switch"
  location: pricing.types.ts:18, pricing.service.ts:214
  why: |
    kind is `string`; switch has no never-default. A new discount kind compiles
    silently and falls through to 0-discount at runtime.
  sketch: |
    ```ts
    type Discount = PercentDiscount | FixedDiscount | PackageDiscount; // kind-discriminated
    default: return assertNever(discount);
    ```
  risk: low
  effort: XS

- id: R-typescript-3
  severity: S2
  title: Exported pricing functions missing return types (implicit-any leak into booking module)
  location: pricing.service.ts:120,214,377; quote.builder.ts:30
  why: |
    Consumers in booking/package modules infer `any` through 2 of these; refactor
    safety across the module boundary is gone.
  sketch: |
    ```ts
    async calculateQuote(tenantId: string, input: QuoteInput): Promise<Quote>
    ```
  risk: low
  effort: XS

- id: R-clean-2
  severity: S2
  title: pricing.service.ts is 812 lines; calculateQuote() is 174 lines at nesting depth 5
  location: pricing.service.ts:1-812 (calculateQuote 120-294)
  why: |
    Three axes of change (quote assembly, discount math, tax/rounding) in one class;
    every pricing change reads 800 lines of context. Extract pure-function modules.
  sketch: |
    ```ts
    // pricing.service.ts keeps DI + orchestration + tenant scoping; delegates to:
    // quote-calculator.ts, discount-engine.ts (R-clean-1), tax.ts — pure, unit-testable
    ```
  risk: medium
  effort: M

- id: R-perf-1
  severity: S2
  title: N+1 — per-line-item `findUnique` inside quote loop
  location: pricing.service.ts:158-171
  why: |
    Quote with n services issues 1+n queries (evidence: 7 queries for a 6-line quote
    in the dev query log). One tenant-scoped `findMany` suffices.
  sketch: |
    ```ts
    const services = await tx.service.findMany({ where: { tenantId, id: { in: serviceIds } } });
    ```
  risk: low
  effort: S

- id: R-extensibility-1
  severity: S2
  title: if/else ladder over discount kinds duplicated at 6 call sites → lookup table
  location: pricing.service.ts:214-262 (+5 mirrored sites)
  why: |
    Each new discount kind touches 6 branches; combined with R-clean-1 this collapses
    to one handler-table entry per kind.
  sketch: |
    ```ts
    const discountHandlers: Record<Discount['kind'], DiscountHandler> = { percent, fixed, package };
    ```
  risk: low
  effort: S

- id: R-clean-3
  severity: S3
  title: "BATCHED naming pass: calcPrice/computeTotal/getFinalAmount + `data`/`info` locals (14 sites)"
  location: pricing.service.ts, quote.builder.ts (14 sites)
  why: |
    Three verbs for one concept; greppability and review speed suffer. Single
    rename-only sweep, zero behaviour change.
  sketch: |
    ```ts
    // one verb: computeX for pure math, buildX for assembly — rename only
    ```
  risk: low
  effort: XS
```

**Rule 18 moment (one-liner in CLAUDE.md → body in RULES.md: removing "dead code" requires proving what it guarded):** clean-code analyzer initially flagged `legacyRoundHalfUp()` (pricing.service.ts:701) as dead. Before proposing deletion: (1) surrounding comment reads *"pre-2025 tenants invoice with half-up rounding; migration left them on this path"*; (2) `git log -p` on the introducing commit confirms it guards a real tenant cohort; (3) it is reached via a config branch, not statically dead. **Finding withdrawn** — recorded under Accepted Risks, not proposed.

## 6. STEP 3 — Aggregate + Rank

Dedupe: typescript-analyzer independently flagged the duplicated discount math → merged into **R-clean-1**. Score = `severity_weight / (risk_weight × effort_weight)` (S1=9, S2=4, S3=1; low=1/med=2/high=4; XS·S=1/M=2/L=4); keep if ≥ 1:

| id | sev | risk | effort | score | keep? |
|---|---|---|---|---|---|
| R-clean-1 | S1(9) | low(1) | S(1) | 9.0 | ✅ |
| R-typescript-1 | S1(9) | low(1) | S(1) | 9.0 | ✅ |
| R-typescript-2 | S2(4) | low(1) | XS(1) | 4.0 | ✅ |
| R-typescript-3 | S2(4) | low(1) | XS(1) | 4.0 | ✅ |
| R-extensibility-1 | S2(4) | low(1) | S(1) | 4.0 | ✅ |
| R-perf-1 | S2(4) | low(1) | S(1) | 4.0 | ✅ |
| R-clean-2 | S2(4) | med(2) | M(2) | 1.0 | ✅ (=1) |
| R-clean-3 | S3(1) | low(1) | XS(1) | 1.0 | ✅ batched |

Anti-pattern guards (SKILL.md) applied:
- **Nit-storm cap:** 14 S3 naming nits collapsed into ONE batched finding (R-clean-3) — S3 ≤10% of total honored by batching.
- **Risk-outweighs-benefit drop (STEP 3):** performance-analyzer's restructure of the stable 200-line tax-band table for minor line savings — **dropped** (the command's own example case, almost verbatim).
- **Test refactoring:** spec files mirror the duplication — **out of scope**, own proposal pass per anti-patterns list.
- **Cross-module escalation check:** all proposals confined to `apps/api/src/modules/pricing/` (≤3 modules) → no architectural escalation.
- **Framework churn:** none proposed (no library swaps).

## 7. STEP 4 — Write the Proposal Document (the ONLY artifact; zero source edits)

```
Write .claude/refactor-proposals/2026-07-06-api-pricing-module.md
```

Document as written (sketches carried inline in full; abbreviated here to the ones shown above):

```markdown
# Refactor Proposal: apps/api/src/modules/pricing

Scope: apps/api/src/modules/pricing/
Generated: 2026-07-06T11:05:00+08:00
Head: 4f2a91c

## Summary
- 8 proposals (2×S1, 5×S2, 1 batched S3): kill 3-way discount-math duplication, remove
  `any` boundary casts, split the 812-line service into pure-function modules.
- All low/medium risk; every accepted item runs through the full MIU pipeline
  (plan → implement → review → test → deliver) — no "it's just a cleanup" skips.
- Zero behaviour change intended; the existing green suite is the oracle (Rule 19:
  tests are never rewritten to match new code as if the old expectation never existed).

## Accepted Risks (deliberately NOT refactored)
- `legacyRoundHalfUp()` (pricing.service.ts:701) — looks dead, is load-bearing for
  pre-2025 tenant invoicing (Rule 18 trace: comment + introducing commit). Untouched.
- Tenant-scoping predicates and the Redis-lock + serializable-tx pattern in callers —
  ADR-backed safety invariants, out of bounds for a cleanup pass.
- Test-suite duplication — real, but test refactors get their own proposal pass.
- Stable 200-line tax-band table — restructuring risk outweighs line savings.
- Architecture analyzer did not run (not opted in). One boundary observation parked
  under Architectural Concerns below.

## Proposals

### R-clean-1 — Extract single discount engine (3-way duplication) [S1 · low · S]
Location: pricing.service.ts:214-262, quote.builder.ts:88-131, package-pricing.helper.ts:40-78
**Why:** 3 divergent copies of discount math; rounding order already disagrees.
**Sketch:** `discount-engine.ts` pure function, one rounding policy. **Routes to:** MIU-refactor-1

### R-typescript-1 — Replace `as any` boundary casts with zod-parsed shapes [S1 · low · S]
Location: pricing.service.ts:341,405,589; stripe-price.mapper.ts:52 → **Routes to:** MIU-refactor-2

### R-typescript-2 — Discriminated union + exhaustive switch for discount.kind [S2 · low · XS]
→ **Routes to:** MIU-refactor-3

### R-typescript-3 — Explicit return types on exported pricing functions [S2 · low · XS]
→ **Routes to:** MIU-refactor-4

### R-extensibility-1 — Discount-kind handler table replaces 6 if/else ladders [S2 · low · S]
Depends on R-clean-1 + R-typescript-2. → **Routes to:** MIU-refactor-5

### R-perf-1 — Batch service lookups (kill N+1 in calculateQuote) [S2 · low · S]
Evidence: 7 queries for a 6-line quote. `findMany` stays tenant-scoped. → **Routes to:** MIU-refactor-6

### R-clean-2 — Split pricing.service.ts (812L) into orchestrator + pure modules [S2 · medium · M]
quote-calculator / discount-engine / tax; service keeps DI + tenant scoping.
→ **Routes to:** MIU-refactor-7

### R-clean-3 — Batched naming pass (14 sites, rename-only) [S3 · low · XS]
→ **Routes to:** MIU-refactor-8

## Architectural Concerns (if any)
> Flagged — requires explicit user approval before queuing. Do NOT auto-route.
- Booking module deep-imports pricing internals (quote.builder, 2 sites). Correcting
  the boundary touches booking + pricing + package → Requires Architectural Review;
  a plain "accept" will NOT queue it — an explicit "I approve the architectural
  change to the pricing module boundary" is required.

## How to Accept
Reply with the proposal IDs to queue, e.g. `accept R-clean-1, R-typescript-1`.
Accepted items become MIUs (8-field format incl. the mandatory Build/Deploy/Runtime
impact field, per skills/miu-methodology) under product task `refactor/api-pricing-module`
and run through the standard pipeline.
```

```
[event] .claude/agent-events.jsonl ← {"t":"refactor-proposal","target":"apps/api/src/modules/pricing",
        "doc":".claude/refactor-proposals/2026-07-06-api-pricing-module.md","findings":8,"dropped":3}
[state] .claude/pipeline-state.json ← {"phase":"refactor-proposal","status":"awaiting-triage"}
```

## 8. STEP 5 — Present + STOP

Handed to the user:

> **Refactor proposal ready:** `.claude/refactor-proposals/2026-07-06-api-pricing-module.md` (HEAD `4f2a91c`)
>
> | ID | Title | Sev | Risk | Effort | Score |
> |---|---|---|---|---|---|
> | R-clean-1 | Single discount engine (3-way duplication) | S1 | low | S | 9.0 |
> | R-typescript-1 | Remove `as any` boundary casts (zod parse) | S1 | low | S | 9.0 |
> | R-typescript-2 | Discriminated union for discount.kind | S2 | low | XS | 4.0 |
> | R-typescript-3 | Return types on exported functions | S2 | low | XS | 4.0 |
> | R-extensibility-1 | Discount handler table (6 ladders → 1) | S2 | low | S | 4.0 |
> | R-perf-1 | Batch service lookups (N+1) | S2 | low | S | 4.0 |
> | R-clean-2 | Split 812-line service into pure modules | S2 | med | M | 1.0 |
> | R-clean-3 | Batched naming pass (14 sites) | S3 | low | XS | 1.0 |
>
> Deliberately not touched: the legacy rounding fallback (load-bearing — Rule 18 trace), tenant-scoping / lock-tx invariants (ADRs), test-suite duplication (own pass), the tax-band table (risk > benefit). One boundary issue is parked under **Architectural Concerns** and needs your explicit architectural approval — a plain "accept" won't queue it.
>
> **I have not modified any source file, created a branch, or committed anything.** Reply with IDs to queue, e.g. `accept R-clean-1, R-typescript-1, R-typescript-2` — accepted items become 8-field MIUs under `refactor/api-pricing-module` and run through the normal pipeline (plan → implement → review → test → deliver), which I'll then kick off via `/dev-pipeline:pipeline` (Rule 3 auto-transition applies from that point).

```
[hook Stop] hooks/stop-review-guard.sh
  → .claude/.auto-review-pending: absent (no commit was made this turn) → turn-end permitted
```

**Session ends here — STEP 5 says STOP and wait.** Explicitly NOT done, per command hard rules:
- ❌ No Edit/Write on any source file — including "the easy ones" (R-typescript-3 is a 4-line change and was still not applied; "Just fix the easy ones" is a named prohibition)
- ❌ No branch, no commit, no push, no PR (Rule 4: delivery only via `/dev-pipeline:deliver`'s gate chain — never reached)
- ❌ No test changes
- ❌ STEP 6 (queue MIUs → invoke `/dev-pipeline:pipeline`) not reached — fires only on the user's accept-IDs reply; the architectural item additionally requires the explicit approval sentence

---

## Protocol-fidelity checklist (self-audit, post-diet plugin)

| Requirement | Source (ad767fe) | Honored |
|---|---|---|
| CHANGELOG skimmed at session start; new entries in force | CLAUDE.md header | ✅ |
| SessionStart hook consumed (no active pipeline, no pending review) | hooks/session-start.sh · Rule 5 | ✅ |
| Trivial-change bypass evaluated BEFORE routing, rejected with reasons | CLAUDE.md ROUTING (new in diet) | ✅ |
| Single routing row matched; classification announced in one line | routing table "Clean up/Refactor/Simplify [X]" | ✅ (no row conflict exists post-diet) |
| Rule one-liners resolved to full bodies where load-bearing | docs/RULES.md Rules 9, 18, 19, 23 | ✅ |
| Socratic gate evaluated, skipped with litmus-based reason | Rule 23 / spec-elicitor | ✅ |
| Clean main / not mid-feature / no dirty tree checked at entry | refactor.md Hard Rules + SKILL.md | ✅ |
| Scope asked ONCE only if not given; whole-repo refused if asked | refactor.md STEP 0 | ✅ (target given → no question) |
| code-refactor skill loaded; banner printed exactly once | SKILL.md activation banner | ✅ |
| Living docs read first; code treated as ground truth (softened precedence) | refactor.md STEP 1 + CLAUDE.md PROJECT KNOWLEDGE | ✅ |
| 4 parallel analyzers in one block; architecture gated off | refactor.md STEP 2 | ✅ |
| Strict finding YAML; sketch ≤20 lines; unactionable dropped | SKILL.md finding format | ✅ |
| Score formula + dedupe + risk-outweighs-benefit drop | refactor.md STEP 3 + SKILL.md scoring | ✅ |
| S3 nit-storm cap; no test refactor; >3-module escalation check; no framework churn | SKILL.md anti-patterns | ✅ |
| Rule 18 trace before proposing any dead-code deletion | RULES.md Rule 18 | ✅ (legacyRoundHalfUp withdrawn) |
| Proposal doc at `.claude/refactor-proposals/<date>-<slug>.md`, mandated structure | refactor.md STEP 4 | ✅ |
| Present + STOP; no edits/branch/commit; wait for accept IDs | refactor.md STEP 5 | ✅ |
| Architectural items never auto-queue on plain "accept" | refactor.md STEP 6 + Rule 9 | ✅ (stated to user) |
| Stop hook passes (no unblessed pending commit) | hooks/stop-review-guard.sh · Rules 2/8 | ✅ |

**Delta vs baseline run (4cc79c3):** the dieted routing table removed the REFACTOR→pipeline vs REFACTOR_PROPOSAL row ambiguity the baseline had to resolve via Rule 9; the new trivial-bypass check added one explicit pre-routing decision; hooks are now real (SessionStart/Stop observed) instead of narrated; rule bodies were fetched from `docs/RULES.md` on demand rather than living in always-on context. Same end state: proposal doc, zero source edits, session stopped awaiting accept IDs.
