# Run: T07 · plugin SHA 4326240 · 2026-07-06

**Task (user message):** "Clean up the pricing module — it's a mess."
**Fixture:** Working monorepo; `apps/api/src/modules/pricing/` is functional (CI green) but messy: 800-line `pricing.service.ts`, duplicated discount logic, `any` casts, inconsistent naming. Repo is dev-pipeline-managed. No open PR. Clean `main`.
**Instruction source:** `/tmp/dp-factory/` plugin checkout only (`CLAUDE.md` + `commands/*` + `skills/*` + `agents/*` + `hooks/*` + `docs/RULES.md`).

---

## SESSION TRANSCRIPT

### [Session start — hooks]

```
[session-start.sh] engineering-craft skill present (~/.claude/skills/engineering-craft, refreshed <24h — rate-limited, no pull)
[session-start.sh] pipeline state: .claude/pipeline-state.json → no active task, no in-progress MIUs, no PR
[session-start.sh] no .claude/.auto-review-pending marker (Stop hook has nothing to guard)
[session-start.sh] co-review: .claude/co-review/enabled NOT present → no nudge (Rule 21: opt-in only)
[session-start.sh] living docs found: .claude/docs/PROJECT_STATUS.md, ARCHITECTURE.md, RECENT_CHANGES.md
```

Per plugin `CLAUDE.md` header ("What's new"), skimmed `CHANGELOG.md`: 2026-07-06 factory-redesign adaptations in force (contract-first MIU ordering, mutation-testing backstop for Rule 19 [opt-in], quality-goals SPEC axis, mechanical MIU-format validator `tools/validate-miu-breakdown.sh`); CLAUDE.md diet + real lifecycle hooks; trivial-change bypass; Rule 22 SDK reality-check; Socratic gate widened across fix/update. All in force.

---

### Turn 1 — user

> Clean up the pricing module — it's a mess.

**Auto-Invocation Decision Tree (checked at start of turn):**
- AUTO-REVIEW DIRECTIVE in hook output? No.
- Active pipeline with in-progress MIUs? No (state file empty).
- User requesting a code change? **Yes → classify.**

**Classification.** Routing table, exact row: "Clean up/Refactor/Simplify [X]" → **REFACTOR_PROPOSAL** → `/dev-pipeline:refactor` (propose-only). Not ambiguous — no classification question needed. Trivial-change bypass considered and rejected: "clean up a module" is not a one-sentence diff touching ≤2 files. Rule 23 Socratic gate considered and skipped: there is no behaviour-in-question here — the module's behaviour is settled (CI green); the request is structural, and `refactor.md` STEP 0 carries its own scoping question instead.

**Announced:** *"Classified as REFACTOR_PROPOSAL → invoking `/dev-pipeline:refactor` (propose-only — per Rule 9 I will produce a proposal document, not edit code)."*

→ Invoking command: **`/dev-pipeline:refactor`**

---

#### STEP 0 — Scope

- **Target path:** given by the user — `apps/api/src/modules/pricing/`. Single module → within the one-module-per-run scope limit (Rule 9); this is NOT a whole-repo sweep, so no refusal/redirect to the scheduled task.
- **Focus tags:** not given → default applies: `style, typescript, performance, maintainability, extensibility` — **`architecture` excluded** (requires explicit opt-in).
- Since target was given and tags have a documented default, the "Ask ONCE" clause is satisfied without a question. Proceeding.

Preconditions (skill: "Never fires mid-feature. Never fires on a dirty working tree."):
```
$ git status --porcelain          → (empty — clean tree)
$ git branch --show-current       → main
$ gh pr list --state open         → no open pull requests
```
Clean `main`, no mid-feature state. ✅

→ Loading `skills/code-refactor/SKILL.md`:

```
🔧 [dev-pipeline] skill: code-refactor — refactor analysis engine active
   Mode: PROPOSE ONLY — no source files will be modified
   Analyzers: typescript | clean-code | performance | extensibility | [architecture: gated]
```

---

#### STEP 1 — Read the Living Docs (never refactor blind)

- `Read .claude/docs/ARCHITECTURE.md` → pricing module owns quote computation + discount composition; consumed by `booking` and `payment` modules via `PricingService.quoteFor()`; DTOs validated by global `forbidNonWhitelisted` pipe.
- `Read .claude/docs/RECENT_CHANGES.md` → last two changes touched `pricing/` (promo-code stacking fix; package-price override) — explains part of the churn/duplication. Docs <1 day old and consistent with code spot-checks → no `/dev-pipeline:sync` needed.

Key shape-explaining fact captured: the duplicated discount logic exists because the promo-stacking hotfix copied `computeDiscount` into the package path rather than generalizing it — so a proposal must unify, not merely delete.

---

#### STEP 2 — Parallel Analysis Sweep (Agent tool, 4 concurrent)

Launched four analyzers in one message (architecture analyzer NOT launched — not in focus tags, user did not opt in):

```
Agent[1] typescript analyzer      → apps/api/src/modules/pricing/**
Agent[2] clean-code analyzer      → apps/api/src/modules/pricing/**
Agent[3] performance analyzer     → apps/api/src/modules/pricing/**  (framework-aware: NestJS/Node rubric, not React)
Agent[4] extensibility analyzer   → apps/api/src/modules/pricing/**
```

Returned findings (strict format `id / severity / title / location / why / sketch / risk / effort`), abridged:

**typescript** —
- `R-typescript-1` · S1 · `as any` casts on Prisma results at the service boundary (`pricing.service.ts:112,340,617`) — erases the compiler on money-bearing fields; enables runtime-only bugs. Sketch: derive `PriceLine` from Prisma payload types, remove casts. risk: low · effort: S
- `R-typescript-2` · S2 · discount kinds are a stringly-typed `discount.type === 'loyalty' | 'promo' | 'package'` with no discriminated union; non-exhaustive `switch` lacks `never` default (`discount.helpers.ts:44`). risk: low · effort: XS
- `R-typescript-3` · S3 · missing return types on two exported helpers. risk: low · effort: XS

**clean-code** —
- `R-clean-1` · S1 · `computeDiscount` duplicated (~38 lines, 92% identical) in `pricing.service.ts:210-248` and `package-pricing.helpers.ts:61-99`; the two copies have ALREADY diverged once (promo-stacking fix applied to only one) — the canonical "divergent bug fixes" S1. Sketch: extract `applyDiscountPolicy(quote, discounts)` into `discount.engine.ts`, both paths call it. risk: medium · effort: M
- `R-clean-2` · S2 · `pricing.service.ts` is 800 lines / one class, three axes of change (quote assembly, discount composition, tax rounding). Sketch: split into `quote-builder.ts`, `discount.engine.ts`, `tax.ts`; service becomes an orchestrator (~150 lines). Incremental, per-seam moves — not a big-bang rewrite. risk: medium · effort: L
- `R-clean-3` · S3 · naming drift: `data`, `info2`, `handlePrice` vs `computeQuote` conventions elsewhere. risk: low · effort: XS

**performance** —
- `R-perf-1` · S2 · candidate N+1: per-line-item `prisma.discountRule.findMany` inside a loop (`pricing.service.ts:455`). **No measurement attached** — analyzer could not quantify (no benchmark in fixture).

**extensibility** —
- `R-ext-1` · S2 · discount-type `if/else` ladder repeated in 3 places; should be a policy lookup table keyed by the discriminated union from R-typescript-2 (natural pairing). risk: low · effort: S
- `R-ext-2` · S3 · speculative "pluggable pricing strategies" seam — **dropped by its own rubric**: speculative extensibility is not proposed unless the user explicitly asked. YAGNI is the hard default.

---

#### STEP 3 — Aggregate + Rank

- Dedupe/merge: `R-ext-1` depends on `R-typescript-2` (union first, then table) — kept as separate proposals with an ordering note (contract-first: the type union is the contract source, per the 2026-07-06 contract-first rule; if accepted, its MIU precedes consumers).
- **Dropped:** `R-ext-2` (speculative, YAGNI) and `R-perf-1` (skill: "Don't propose perf work without evidence" — no measured latency/query count; recorded under Accepted Risks with a note to re-raise WITH a measurement, e.g. Prisma query logging on a representative quote).
- Rank by `severity_weight × 1/risk × 1/effort`: R-typescript-2 → R-typescript-1 → R-ext-1 → R-clean-1 → R-clean-3 → R-typescript-3 → R-clean-2.
- Risk-vs-benefit check on the biggest item (R-clean-2, the 800-line split): benefit is real (three axes of change in one file, already causing the duplication), risk is contained by proposing per-seam extraction with tests at each step → retained, marked medium-risk/L and sequenced LAST behind the smaller de-risking proposals.

---

#### STEP 4 — Write the Proposal Document

`Write .claude/refactor-proposals/2026-07-06-pricing.md` (abridged):

```markdown
# Refactor Proposal: pricing module

Scope: apps/api/src/modules/pricing/
Generated: 2026-07-06T10:42:00Z
Head: 20d8d63

## Summary
- Duplicated discount logic has already caused one divergent fix; unify into a single discount engine (R-clean-1).
- `any` casts + stringly-typed discount kinds erase compile-time safety on money paths (R-typescript-1/2, R-ext-1).
- The 800-line service splits cleanly along three existing seams — incremental extraction, not a rewrite (R-clean-2).

## Accepted Risks
- NO architecture-tag analysis ran (not opted in); module boundaries and data flow untouched.
- R-perf-1 (suspected N+1 at pricing.service.ts:455) NOT proposed — no measurement; re-raise with Prisma query-log evidence.
- R-ext-2 (pluggable strategy seam) NOT proposed — speculative (YAGNI).
- Public surface `PricingService.quoteFor()` signature is frozen — consumers in booking/payment are out of scope.

## Proposals

### R-typescript-2 — Discriminated union for discount kinds  [S2 · low-risk · XS]
Location: `apps/api/src/modules/pricing/discount.helpers.ts:44`
**Why:** stringly-typed kinds + non-exhaustive switch; adding a kind fails silently at runtime.
**Sketch:**
    type Discount =
      | { kind: 'loyalty';  tier: LoyaltyTier }
      | { kind: 'promo';    code: string; pct: number }
      | { kind: 'package';  packageId: string };
    // switch(d.kind) { ... default: assertNever(d) }
**Routes to:** MIU-refactor-1 (contract source — precedes R-ext-1/R-clean-1 if accepted)

### R-typescript-1 — Remove `as any` at Prisma boundary  [S1 · low-risk · S]
Location: `pricing.service.ts:112,340,617` — derive `PriceLine` from Prisma payload types. → MIU-refactor-2

### R-ext-1 — Discount if/else ladders → policy table  [S2 · low-risk · S]
Location: 3 sites — `Record<Discount['kind'], DiscountPolicy>` lookup. Depends on R-typescript-2. → MIU-refactor-3

### R-clean-1 — Unify duplicated computeDiscount  [S1 · medium-risk · M]
Location: `pricing.service.ts:210-248` + `package-pricing.helpers.ts:61-99` — extract `discount.engine.ts`;
golden-master tests over both call paths BEFORE unifying (the copies diverged once already — tests must
pin current behaviour of EACH path first). → MIU-refactor-4

### R-clean-3 — Naming pass  [S3 · low-risk · XS] → MIU-refactor-5
### R-typescript-3 — Return types on exported helpers  [S3 · low-risk · XS] → MIU-refactor-6

### R-clean-2 — Split 800-line service along its 3 seams  [S2 · medium-risk · L]
quote-builder.ts / discount.engine.ts / tax.ts; service becomes orchestrator. Sequenced last;
per-seam moves, each behind the golden-master suite from R-clean-1. → MIU-refactor-7

## Architectural Concerns (if any)
None flagged — architecture analyzer did not run (not opted in).

## How to Accept
Reply with the proposal IDs to queue, e.g. `accept R-typescript-2, R-clean-1`.
Accepted items become MIUs and run through the standard pipeline.
```

---

#### STEP 5 — Present + Wait (STOP)

Presented to the user:

> **Refactor proposal written:** `.claude/refactor-proposals/2026-07-06-pricing.md` (HEAD 20d8d63)
>
> | ID | Title | Sev | Risk | Effort |
> |---|---|---|---|---|
> | R-typescript-2 | Discriminated union for discount kinds | S2 | low | XS |
> | R-typescript-1 | Remove `as any` at Prisma boundary | S1 | low | S |
> | R-ext-1 | Discount ladders → policy table | S2 | low | S |
> | R-clean-1 | Unify duplicated computeDiscount (+ golden-master tests first) | S1 | med | M |
> | R-clean-3 | Naming pass | S3 | low | XS |
> | R-typescript-3 | Return types on exported helpers | S3 | low | XS |
> | R-clean-2 | Split 800-line service along 3 seams | S2 | med | L |
>
> **Dropped (recorded in Accepted Risks):** R-perf-1 (suspected N+1 — no measurement; needs evidence), R-ext-2 (speculative seam — YAGNI). Architecture analysis did not run (opt-in only — say the word if you want it).
>
> **Nothing was changed:** no source edits, no branch, no commit — per Rule 9 this flow is propose-only.
>
> Reply with the IDs to queue, e.g. `accept R-typescript-2, R-typescript-1, R-clean-1`. Accepted items become MIUs (8-field format, contract-first ordering, validated by `tools/validate-miu-breakdown.sh`) under product task `refactor/pricing` and run through the standard pipeline gates (implement → review-blessing → validate → deliver).

**Session ends here — waiting on the user.** Deliberately NOT done (per `refactor.md` STEP 5 hard list): no source file edited, no branch created, no commit, no "just fix the easy ones" (the two XS items were NOT applied despite being trivial — the trivial-change bypass does not override an already-routed REFACTOR_PROPOSAL, and Rule 9 requires acceptance by ID). Stop hook check: no `.claude/.auto-review-pending` marker (no commit was made) → turn may end.

---

## WHAT WAS / WASN'T CHANGED

| Changed | Not changed |
|---|---|
| `.claude/refactor-proposals/2026-07-06-pricing.md` (new proposal doc) | Any file under `apps/api/src/modules/pricing/` |
| `.claude/agent-events.jsonl` (routing + analyzer audit entries) | Git state: no branch, no commit, no push, no PR |
|  | `.claude/miu-progress.json` (MIUs queue only on acceptance — STEP 6) |

## HANDOFF

User holds the ball: accept proposals by ID → STEP 6 queues MIUs under `refactor/pricing` and invokes `/dev-pipeline:pipeline`. Any architecture-level change would additionally require an explicit "I approve the architectural change to X" (none pending — analyzer gated off).
