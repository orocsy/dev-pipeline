# Review findings — PR #3 Codex round 2 (via co-review)

| # | Severity | File:Line | Reviewer | Issue | Proposed Fix |
|---|----------|-----------|----------|-------|--------------|
| 9 | P1 | verify-sdk-surface.md STEP0/Signal C | codex | All 4 detection regexes + Signal C require `import`/`from` on ONE line; a multiline import (`import {\n  x\n} from 'pkg'`) matches nothing -> STEP 0 skips the WHOLE gate | Collapse multiline `import { ... } from 'pkg'` blocks onto one logical line before regex matching |
| 6 | P1 | verify-sdk-surface.md STEP5 (missing-probe) | codex | The missing-probe diagnostic is an `&&`-gated `echo`, nothing sets `$P1` from it; STEP 5 can report a clean PASS even with unprobed surfaces | Accumulate `P1=$(( ${P1:-0} + (SURFACES-PROBED) ))` after the diagnostic |
| 8 | P1 | verify-sdk-surface.md STEP1c (BINDINGS) | codex | Binding extraction only scans ES `import...from` lines; `require('pkg')`-assignment bindings (`const sdk = require('pkg')`) are never extracted, so `$SURF_FILE` stays empty for that call style even though Signal C lets the gate fire | Extend BINDINGS regex to also capture `const/let/var X = require(...)` LHS identifiers |
| 10 | P1 | verify-sdk-surface.md STEP2 (consumption check) | codex | The shape-mismatch consumption grep still hard-requires `\.$METHOD\(` (member-call only); round 1 added bare/constructor-call detection to STEP 1c's surface list but never updated this SEPARATE check, so a bare `getUploadMetadata(...)` call's shape is never verified — the exact production-500 pattern | Add a bare-call consumption pattern mirroring STEP 1c's, feed through the same shape comparison |
| 2 | P1 | review.md STEP1.6 trigger | codex | The caller-side trigger (`.d.ts` file OR literal `import `/`require(` in added lines) doesn't fire for a diff that only adds a call on an already-imported package -- so verify-sdk-surface (even with its Signal C fix) is never invoked at all from review.md | Broaden the trigger to also fire on any added call-like line, or invoke verify-sdk-surface unconditionally and trust its own STEP 0 to skip cleanly |
| 1 | P1 | verify-sdk-surface.md STEP0 (BASE range) | codex | `$BASE..HEAD` excludes the working tree/index; if invoked pre-commit (as validate.md's wiring implies) a staged-but-uncommitted SDK call is invisible | Add a worktree-inclusive `git diff "$BASE"` source alongside the HEAD-bounded one |
| 11 | P2 | verify-sdk-surface.md STEP2 (find) | codex | `find node_modules -path "*$PKG*"` is an unanchored substring match; short unscoped names (e.g. `ms`) can match unrelated nested packages, producing a false "method exists" pass | Resolve the exact install dir via `require.resolve('$PKG/package.json')` first, scope `find` to it |
| 3 | P2 | co-review-adapter.md:127 (cursorAfter) | codex | Prose names `git rev-parse HEAD:<doc>` (a BLOB sha) as the cursor before parenthetically giving the correct `git log -1 --format=%H -- <doc>` (commit sha) -- an implementer following the primary clause verbatim persists the wrong value | Delete the misleading blob-sha clause, keep only the commit-sha form |
| 5 | P2 | co-review-adapter.md doc detect() (empty cursor) | codex | `"$CURSOR"..HEAD` with empty `$CURSOR` expands to `..HEAD` = `HEAD..HEAD` (empty range), not "whole doc is new" as documented -- breaks first-run detection on a pre-existing (not-yet-scaffolded-by-PHASE-0) doc | Branch on empty `$CURSOR`: diff against the empty-tree SHA, or parse all `## Round N` sections directly |
| 12 | P2 | co-review.md:21 (flag parsing) | codex | `$1` is taken as CHANNEL unconditionally with no `--flag` stripping; `co-review --watch` sets CHANNEL="--watch", scaffolding a nonsense config instead of enabling watch mode | Siphon leading `--*` tokens into a flags var before assigning CHANNEL from the first non-flag arg |
| 7 | P2 | verify-sdk-surface.md STEP4 vs STEP5 (probe path) | codex | STEP 4's documented fallback is per-package `.claude/sdk-probes/<pkg>.md`; STEP 5 hard-codes the singular `.claude/sdk-probes/probe.md` -- a project using the documented fallback is silently never read, every surface reports unprobed | Glob `.claude/sdk-probes/*.md` in STEP 5 (or align STEP 4's fallback naming to the singular file) |
| 4 | P3 | hooks/co-review-nudge.sh (gh-pr-bot case) | codex | Nudge only queries PR comments, never PR reviews -- a review-only Codex pass (no inline comments) never surfaces the CO-REVIEW PENDING nudge | Add a second `gh api pulls/$_pr/reviews` count into `_n` |
