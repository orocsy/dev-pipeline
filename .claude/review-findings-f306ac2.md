# Review findings — PR #3 Codex round 1 (via co-review)

| # | Severity | File:Line | Reviewer | Issue | Proposed Fix |
|---|----------|-----------|----------|-------|--------------|
| 1 | P1 | commands/verify-sdk-surface.md:41 | codex | STEP 0 only detects NEW import/require/`.d.ts` lines; a diff that adds a `.method(` call on an ALREADY-imported package (no new import, no `.d.ts` edit) never trips PKG_IMPORTS/STUB_HITS, so the gate silently skips exactly the case Rule 22 targets | Also scan added lines for `.<method>(` calls on bindings from the file's *existing* (unchanged) import lines, not just newly added ones |
| 2 | P2 | commands/co-review.md:186-193 (REAPPEARED check) | codex | PHASE 4 step 3 appends the CURRENT round's resolvedIds to roundHistory BEFORE PHASE 5 evaluates REAPPEARED, so PHASE 5's `[.roundHistory[].resolvedIds[]]` includes THIS round's own ids — any finding resolved this round is counted as "reappeared," stalling after round 1 | Scope the lookup to prior rounds only (`roundHistory[0:-1]`), or evaluate REAPPEARED before the current round's entry is appended |
| 3 | P2 | commands/verify-sdk-surface.md:143-163 (probe join) | codex | Two independent file-wide `grep -qF` checks (package name anywhere, method name anywhere) can match on DIFFERENT rows of a multi-row SDK-PROBE.md, falsely marking a surface as probed | Check package+method on the SAME table row (parse row-by-row, or a single regex requiring both fields on one `|`-delimited line) |
| 4 | P2 | commands/co-review.md:21 | codex | On the very first run with no `$1` and no existing config, `$CHANNEL` resolves to empty string, so the scaffold writes `.claude/co-review/.json` — a dotfile the `*.json` glob discovery never matches on the next run | Fall back to a real default channel name (e.g. `"default"`) when `$1` is empty and no configs exist |
| 5 | P2 | agents/co-review-adapter.md:82 | codex | The inline-reply `gh api` path is missing the `$PR` path segment — should be `repos/$REPO/pulls/$PR/comments/<id>/replies` per GitHub's documented endpoint, not `repos/$REPO/pulls/comments/<id>/replies` | Add the `$PR` segment to the reply endpoint |
| 6 | P2 | commands/co-review.md:112 (turn guard) | codex | For doc channels, `$CUR`'s cached `turn` is Claude-local; the other agent flips `<!-- TURN -->` in the doc itself, not the cursor file — using the stale cached value means Claude may never see that it's actually its turn | Before the turn guard, `git pull` + re-read the live `<!-- TURN: ... -->` marker from the doc file and use THAT, not `$CUR`'s cached copy |
| 7 | P2 | commands/verify-sdk-surface.md:69 | codex | Surface enumeration only greps `.<method>(` on an import binding — misses bare named-import function calls (`format(...)` after `import { format } from 'date-fns'`) and constructor calls (`new S3Client(...)`), so a real third-party surface can ship unprobed | Add regex patterns for bare-call (`\b<binding>\(`) and constructor-call (`new <binding>\(`) forms alongside the existing `.<method>(` pattern |
| 8 | P3 | commands/verify-sdk-surface.md:66 | codex | The `echo "skip $PKG (workspace symlink)"` diagnostic is redirected into the SAME file (`.claude/co-review-thirdparty-packages.tmp`) as real package names via the loop's stdout redirect — a later consumer could treat "skip ... (workspace symlink)" as a package entry | Redirect the skip diagnostic to stderr (`>&2`) instead of letting it flow into the accumulated stdout |
| 9 | P2 | agents/co-review-adapter.md:76 | codex | The `gh api repos/$REPO/pulls/$PR/reviews` call is NOT paginated (unlike the comments call right above it); GitHub paginates reviews at 30/page, so a PR with >30 review events would silently miss newer bot reviews | Add `--paginate` to the reviews call, matching the comments call |
